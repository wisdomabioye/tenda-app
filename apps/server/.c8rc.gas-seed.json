{
  "//": "Scoped coverage gate for the EVM gas seed + chain-id-keyed seam (#53a). Mirrors .c8rc.contracts.json: the repo-wide gate in .c8rc.json sits at 81/82/80/81, and raising it globally is a separate decision that would fail on unrelated files, so this asserts the house bar (>90) on the code this work owns. Run via `pnpm test:coverage:gas-seed`. NOTE: src/lib/onboarding-deps.ts is deliberately absent \u2014 #53a owns only its two gas-seed functions (covered by test/unit/gas-seed-trigger.test.ts, which runs in this suite list), while the rest of that file is the OTP wiring, whose tests are not in this scoped run. c8 includes whole files, so listing it would measure code this gate does not speak for.",
  "all": true,
  "src": [
    "src"
  ],
  "include": [
    "src/chains/gas-seed-senders.ts",
    "src/chains/evm/gas-seed-sender.ts",
    "src/chains/evm/hot-wallet.ts",
    "src/lib/gas-seed.ts",
    "src/db/seed/rows.ts"
  ],
  "exclude": [
    "src/**/*.d.ts"
  ],
  "reporter": [
    "text"
  ],
  "report-dir": "coverage/gas-seed",
  "clean": true,
  "check-coverage": true,
  "lines": 90,
  "branches": 90,
  "functions": 90,
  "statements": 90
}
