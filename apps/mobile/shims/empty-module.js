// Empty CommonJS stub used by metro.config.js to satisfy `require('crypto')`
// and other Node built-in imports inside @metamask/connect-multichain and its
// transitive deps. None of those Node-specific APIs are actually exercised on
// RN at runtime — the imports just need to resolve.
module.exports = {}
