import { ComingSoon } from '@/components/app/ComingSoon'

export default function MessagesPlaceholderPage() {
  return (
    <ComingSoon
      title="Messages"
      stage={5}
      blurb="Realtime chat with escrow context rides the WebSocket stage."
    />
  )
}
