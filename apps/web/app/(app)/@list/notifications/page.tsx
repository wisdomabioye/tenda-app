import { NotificationsListColumn } from '@/components/notifications/NotificationsListColumn'

/** The @list slot for the notification centre — one entry per depth. */
export default function NotificationsListSlot() {
  return <NotificationsListColumn />
}
