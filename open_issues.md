# Tenda Backend — Open Issues

> Issues identified through code review and audit sessions.
> Fixed issues are kept for historical reference.

---

## Legend

| Symbol | Meaning |
|--------|---------|
| 🔴 | Critical — fix before any real traffic |
| 🟠 | Significant — fix before production launch |
| 🟡 | Minor — fix before scaling or as time permits |
| ✅ | Resolved |

---

## Open

### 🔴 Critical

| # | Issue | File(s) | Fix |
|---|-------|---------|-----|
| ~~F1~~ | ~~Cancel gig UPDATE has no status guard on either path~~ | ✅ Added `eq(gigs.status, 'draft')` / `eq(gigs.status, 'open')` to both UPDATE WHERE clauses; added `if (!cancelledGig) return null` guard inside the open-cancel transaction before the `gig_transactions` INSERT; both paths return 409 on 0 rows | — `DELETE /v1/gigs/:id` in `gigs/_id/index.ts` does SELECT → check status → UPDATE, but neither the draft path (line 284 `where(eq(gigs.id, id))`) nor the open path (line 263 `where(eq(gigs.id, id))`) includes a status guard in the UPDATE's WHERE clause; a concurrent publish racing the draft-cancel, or a concurrent accept racing the open-cancel, would corrupt the gig state and potentially strand escrow funds with no recovery transaction recorded | `apps/server/src/routes/v1/gigs/_id/index.ts` | Add `eq(gigs.status, 'draft')` to draft-cancel UPDATE WHERE and `eq(gigs.status, 'open')` to open-cancel UPDATE WHERE; return 409 on 0 rows like all other TOCTOU-guarded routes |
| ~~F2~~ | ~~PATCH draft gig UPDATE has no status guard~~ | ✅ Added `eq(gigs.status, 'draft')` to the UPDATE WHERE clause; returns 409 `GIG_WRONG_STATUS` on 0 rows | — `PATCH /v1/gigs/:id` checks `gig.status !== 'draft'` after SELECT, but the UPDATE at line 188 uses only `where(eq(gigs.id, id))`; a concurrent publish that transitions draft → open between the SELECT and UPDATE lets the PATCH overwrite `payment_lamports`, `category`, or other fields on a live open gig with a funded escrow | `apps/server/src/routes/v1/gigs/_id/index.ts` | Add `eq(gigs.status, 'draft')` to the PATCH UPDATE WHERE clause; return 409 on 0 rows |
| A1 | **`requireRole` guard is an exact string match — role migration breaks all in-flight admin sessions** — `guards.ts` checks `request.user.role !== role`; after renaming `'admin'` → `'super_admin'` in the DB, every admin JWT issued before the migration (up to 7-day expiry) carries `role: 'admin'` and will be rejected by every admin route guard; no transition strategy exists | `apps/server/src/lib/guards.ts`, `apps/server/src/routes/v1/admin/index.ts` | Change `requireRole` to accept `AdminRole[]` and use `roles.includes(request.user.role)`; accept `'admin'` as an alias for `'super_admin'` for one token lifetime post-migration; coordinate a forced re-login for all admins at migration time (rotate JWT secret briefly or communicate directly) |
| A2 | **Demoted admin retains elevated role in JWT for up to 7 days** — `requireRole` reads `request.user.role` from the JWT claim; when an admin is demoted (e.g., `dispute_resolver` → `user`), they can continue signing on-chain dispute resolutions until their token expires; the `authenticate` decorator's in-process status cache refreshes `status` from DB on cache miss but never refreshes `role` | `apps/server/src/plugins/auth.ts`, `apps/server/src/lib/guards.ts` | Extend the status cache entry to also store `role`; refresh it alongside `status` on cache miss; `requireRole` validates against the cache-refreshed DB role instead of the stale JWT claim |
| A3 | **No CORS restriction on admin routes** — the global CORS plugin allows any origin in `CORS_ORIGIN`; admin endpoints under `/v1/admin/` are reachable from any allowed origin, including mobile web views or third-party pages that hold a stolen admin JWT | `apps/server/src/plugins/cors.ts`, `apps/server/src/app.ts` | Register a scoped CORS plugin on the `/v1/admin` prefix restricted to `ADMIN_ORIGIN` env var; mobile clients should never call admin routes |
| A4 | **`resolve_dispute` endpoint trusts `winner` from client request body — not derived on-chain** — `POST /admin/disputes/:id/resolve { winner, signature }` stores the winner the client claims was signed; `verifyTransactionOnChain` only checks discriminator, not instruction arguments; a client sending `winner: 'poster'` with a signature that encoded `winner: 'worker'` would record the wrong outcome | `apps/server/src/routes/v1/gigs/_id/resolve/index.ts` (and planned admin resolve endpoint) | Fetch the confirmed transaction after verification, decode the borsh-encoded instruction arguments, derive `winner` from the on-chain data; do not accept `winner` as a trusted request body field |
| E1 | **Subscription fan-out is unbounded and in-process** — the `gig.created` listener in `plugins/notifications.ts` fetches all matching subscriptions and calls `sendPush` synchronously in the request path; with thousands of subscriptions and O(n) Expo HTTP calls, a single gig post blocks the event loop and can time out the originating request; no retry on partial delivery failure | `apps/server/src/plugins/notifications.ts`, `apps/server/src/lib/push.ts` | Move fan-out to a background queue (QStash, BullMQ, or pg-based job table); process in batches of 100 with retry; decouple from the HTTP request path entirely |

### 🟠 Significant

| # | Issue | File(s) | Fix |
|---|-------|---------|-----|
| 22 | Module-level caches don't work across multiple server instances — `getPlatformConfig` 5-min cache and `batchExpireGigs` 60s throttle live in process memory; each pod has independent state | `lib/platform.ts`, `lib/gigs.ts` | Replace with Redis or Postgres advisory locks before horizontal scaling |
| ~~G1~~ | ~~**Cloudinary proof upload signature is not scoped to the requesting user**~~ | ✅ `generateUploadSignature` now accepts `userId`; proof signatures use folder `tenda/proofs/${userId}`; `upload/index.ts` passes `request.user.id` for proof type; `submit/index.ts` validates each URL's pathname contains `/tenda/proofs/${userId}/` — cross-user proof submission rejected with 400 |
| ~~G2~~ | ~~`approve/index.ts` always records `fee_bps` for seeker posters, not `seeker_fee_bps`~~ | ✅ `effectiveFeeBps = request.user.is_seeker ? config.seeker_fee_bps : config.fee_bps` — recorded fee now matches the on-chain deduction | — `computePlatformFee(BigInt(gig.payment_lamports), config.fee_bps)` at line 76 uses the standard fee regardless of whether the poster is a seeker; the on-chain smart contract reads `gig_escrow.is_seeker` and applies `seeker_fee_bps` when paying out; the `platform_fee_lamports` value stored in `gig_transactions` will therefore be higher than the actual fee deducted on-chain, producing incorrect financial records | `apps/server/src/routes/v1/gigs/_id/approve/index.ts` | Use `request.user.is_seeker ? config.seeker_fee_bps : config.fee_bps` when computing `platform_fee_lamports`; same fix needed in any other route that records a platform fee |
| ~~F3~~ | ~~`GET /v1/conversations` has no pagination and makes N+2 queries~~ | ✅ Added `LIMIT 50`; replaced per-row loop with 4 fixed bulk queries: conversations, other-user profiles (`IN`), unread counts (`COUNT … GROUP BY`), last messages (`DISTINCT ON`) — query count is now O(1) regardless of conversation volume | — the conversations list fetches all active conversations for the user with no LIMIT, then for each row makes 2 additional queries (other-user profile + last 20 messages); a user with 100 conversations triggers 201 sequential-ish DB queries per page load; at 200 conversations the response payload also becomes unbounded | `apps/server/src/routes/v1/conversations/index.ts` | Add `LIMIT 50` (or cursor pagination) to the conversations query; replace the per-conversation message query with a single lateral join or `DISTINCT ON` query to fetch the last message + unread count in one pass |
| ~~F4~~ | ~~Unread count silently capped at 20~~ | ✅ Replaced `.limit(20)` + in-memory filter with a SQL `COUNT(*)::int GROUP BY conversation_id` query across all unread messages — count is now exact regardless of volume | — the conversations list fetches `.limit(20)` messages per conversation then counts `sender_id !== userId && !read_at` in-memory; a conversation with 21 unread messages shows badge "20" — the actual count is wrong | `apps/server/src/routes/v1/conversations/index.ts` lines 62–71 | Replace in-memory count with SQL aggregate: `COUNT(*) FILTER (WHERE sender_id != $userId AND read_at IS NULL)` on a separate query (or lateral join) so the count is always accurate regardless of message volume |
| ~~F5~~ | ~~`seeker_fee_bps` is immutable via API~~ | ✅ Added `seeker_fee_bps` to `PATCH /v1/admin/platform-config` body with 0–10 000 integer validation; "at least one field" guard updated to include it | — the field exists in `platform_config`, is used by mobile to compute fees, but the admin `PATCH /v1/admin/platform-config` body only accepts `fee_bps` and `grace_period_seconds`; changing `seeker_fee_bps` requires a direct DB statement | `apps/server/src/routes/v1/admin/index.ts`, `apps/server/src/routes/v1/platform/index.ts` | Add `seeker_fee_bps` to the PATCH body with the same 0–10 000 validation as `fee_bps`; update the Body type annotation and the validation block |
| ~~E2~~ | ~~`verifyTransactionOnChain` has no network timeout~~ | ✅ Added `withRpcTimeout()` helper (10 s default); both `getSignatureStatus` and `getTransaction` calls are raced against the timeout; returns `{ ok: false, error: 'RPC_TIMEOUT' }` on expiry | — both `getTransaction` and `confirmTransaction` await the Solana RPC node with no `AbortSignal` or timeout option; a slow/unresponsive RPC node hangs the request handler indefinitely, exhausting the Fastify connection pool under modest concurrency | `apps/server/src/lib/solana.ts` | Wrap each RPC call with `Promise.race([call, timeout(10_000)])` or pass `{ signal: AbortSignal.timeout(10_000) }` via the `Connection` options; circuit-break after repeated failures |
| ~~E3~~ | ~~Country + remote OR filter lacks a covering index~~ | ✅ Root cause was a logic bug: `country` filter incorrectly bundled `OR remote = true`, forcing a full table scan. Fixed to `eq(gigs.country, country)` — country and remote are now independent conditions. Existing `gigs_status_country_idx` and `gigs_status_remote_idx` cover both filter paths; no new migration needed | — `GET /v1/gigs?country=NG` ran `WHERE (country = $1 OR remote = true) AND status = 'open'`; Postgres cannot use a single-column index for an OR across two different columns; full table scans degrade linearly with gig count | `apps/server/src/routes/v1/gigs/index.ts`, `apps/server/src/db/schema.ts` | Add a partial index `CREATE INDEX gigs_open_remote_idx ON gigs(country) WHERE status = 'open' AND remote = false` and a separate `WHERE remote = true AND status = 'open'` index; or denormalise with a generated column; consider PostGIS for the geo filter long-term |
| ~~24~~ | ~~No push notifications or webhooks~~ | ~~Missing~~ | ✅ Implemented: Expo push via typed EventEmitter + `lib/push.ts`; notifications plugin registers listeners for accept/submit/approve/dispute/resolve/message/new-gig events; mobile registers device token on login/session load |
| 81 | **Single admin key for dispute resolution** — `platform_state.admin` is a single `Pubkey`; one compromised key can resolve all disputes arbitrarily with no multi-sig, timelock, or appeal path | `tenda-escrow/…/resolve_dispute.rs`, `tenda-escrow/…/state/platform.rs` | Replace `admin` with a Squads multisig address at deploy time (no contract change needed); longer-term, migrate to on-chain multi-sig constraint |
| 83 | **No approval timeout after `submit_proof`** — once a worker submits proof the gig enters `Submitted` with no deadline for the poster to act; a malicious poster can stall indefinitely, forcing the worker to open a dispute (paying gas and waiting for admin) to escape | `tenda-escrow/…/state/escrow.rs`, `tenda-escrow/…/approve_completion.rs` | Add an `approval_deadline` computed at submission time (`submitted_at + approval_duration_seconds`); allow worker to call a new `claim_stalled_payment` instruction after the deadline passes |

### 🟡 Minor

| # | Issue | File(s) | Fix |
|---|-------|---------|-----|
| ~~N12~~ | ~~**Auth re-login clears stored country when device doesn't send one**~~ | ✅ `onConflictDoUpdate` now uses `...(country ? { country } : {})` — country only overwritten when a non-null value is provided; re-login without locale detection preserves the user's existing country | — `set: { country: country ?? null }` on every login always wrote `null` when `country` was absent; a user who set their country in the profile screen would have it silently cleared on next re-login | `apps/server/src/routes/v1/auth/index.ts` | Use `...(country ? { country } : {})` in the `onConflictDoUpdate` set so country is only updated when a non-null value is received |
| ~~N13~~ | ~~**`before_id` cursor not scoped to current conversation**~~ | ✅ Added `eq(messages.conversation_id, id)` to the cursor lookup WHERE clause | — `GET /v1/conversations/:id/messages?before_id=X` looked up the cursor message by ID only (`WHERE id = before_id`); passing a valid message UUID from a different conversation returned the wrong `created_at`, causing pagination gaps or returning messages from the wrong window | `apps/server/src/routes/v1/conversations/_id/messages/index.ts` | Add `and(eq(messages.id, before_id), eq(messages.conversation_id, id))` to the cursor SELECT |
| ~~N14~~ | ~~**Wallet "Earned" overstates worker payout by platform fee**~~ | ✅ Changed reduce to `sum + tx.amount_lamports - tx.platform_fee_lamports` — earned reflects actual net payout | — `earnedLamports` summed `tx.amount_lamports` for `release_payment` and `dispute_resolved`; `amount_lamports` is the gross payment, but the worker's net is `payment - platform_fee`; at 2.5% fee on a 1 SOL gig the balance was overstated by 0.025 SOL per gig | `apps/mobile/app/(tabs)/wallet.tsx` | Subtract `platform_fee_lamports` in the reduce |
| ~~N15~~ | ~~**Cancel and expired-refund transactions record wrong fee for seeker gigs/offers**~~ | ✅ `cancel` and `refund` routes now use `effectiveFeeBps = request.user.is_seeker ? config.seeker_fee_bps : config.fee_bps` — recorded escrow amount matches what was actually locked on-chain | — `computePlatformFee(payment, config.fee_bps)` was used to reconstruct the escrow total in `cancel_refund` and `expired_refund` records; seeker gigs were published with `seeker_fee_bps` (cheaper), so the recorded `amount_lamports` was too high — DB transaction history disagreed with on-chain data | `apps/server/src/routes/v1/gigs/_id/index.ts`, `apps/server/src/routes/v1/gigs/_id/refund/index.ts`, `apps/server/src/routes/v1/exchange/_id/index.ts` | Use `request.user.is_seeker ? config.seeker_fee_bps : config.fee_bps` for the fee calculation |
| ~~N16~~ | ~~**`GET /v1/users/:id/transactions` ordered oldest-first**~~ | ✅ Changed to `desc(gig_transactions.created_at)` — newest transactions returned first | — `.orderBy(gig_transactions.created_at)` (ascending) returned the oldest transaction first; wallet history shows oldest entries at top — counterintuitive | `apps/server/src/routes/v1/users/_id/transactions/index.ts` | Change to `.orderBy(desc(gig_transactions.created_at))` |
| ~~N17~~ | ~~**Admin self-demotion threw 400 instead of 403**~~ | ✅ Changed to `new AppError(403, …)` — matches the FORBIDDEN error code | — `throw new AppError(400, ErrorCode.FORBIDDEN, …)` in the role-update route used status 400 (bad request) for an authorisation failure; clients that check `statusCode` to determine error type would misclassify it | `apps/server/src/routes/v1/admin/index.ts` | Change 400 → 403 |
| ~~N18~~ | ~~**`GET /v1/admin/blocked-keywords` returned all rows unbounded**~~ | ✅ Added `limit`/`offset` query params (default 50, max `MAX_PAGINATION_LIMIT`) and a parallel COUNT; returns paginated `{ data, total, limit, offset }` | — no `LIMIT` applied; as the keyword list grows the response payload grows proportionally; at 10 000 keywords a single request returns megabytes of data | `apps/server/src/routes/v1/admin/index.ts` | Add `limit`/`offset` pagination matching other list endpoints |
| N19 | **`wallet.tsx` `accept_gig` transactions show raw key** — `TYPE_LABEL` map was missing `accept_gig`; display fell back to the raw DB key string | `apps/mobile/app/(tabs)/wallet.tsx` | ✅ Added `accept_gig: 'Gig accepted'` entry to `TYPE_LABEL` |
| A5 | **Next.js `middleware.ts` runs on Edge Runtime — Node.js JWT libraries will fail at import** — the planned admin panel uses `middleware.ts` to redirect unauthenticated users; Next.js middleware runs on the V8 Isolate (Edge Runtime) which does not support Node.js crypto APIs; `@fastify/jwt`, `jsonwebtoken`, and any Node.js-only package will throw at runtime | `apps/admin/middleware.ts` (planned) | Use `jose` (fully Edge-compatible JOSE implementation) for JWT verification in middleware: `import { jwtVerify } from 'jose'`; do not use `jsonwebtoken` or `@fastify/jwt` in any Next.js middleware or Edge Route Handler |
| A6 | **No index on `users.wallet_address` for prefix search** — `GET /admin/users?q=prefix` will search wallet addresses with `LIKE 'prefix%'`; without a `text_pattern_ops` index Postgres falls back to a sequential scan on the users table; degrades linearly as user count grows | `packages/shared/src/db/schema.ts` | Add via raw SQL migration: `CREATE INDEX users_wallet_prefix_idx ON users (wallet_address text_pattern_ops);` — Drizzle does not natively support `text_pattern_ops` operator class so this must be a raw SQL migration step |
| A7 | **`POST /v1/blockchain/admin/build-resolve` has no role guard** — this endpoint lives under `/v1/blockchain/` (not `/v1/admin/`), so the scoped admin CORS plugin and any prefix-level role guard do NOT cover it; any authenticated user with a valid JWT can call it and receive an unsigned dispute resolution transaction | `apps/server/src/routes/v1/blockchain/admin/build-resolve.ts` (planned) | Add `requireRole('dispute_resolver', 'super_admin')` explicitly on this handler — do not rely on prefix-level guards |
| A8 | **`airdrop_campaigns.amount_lamports` uses `mode: 'bigint'` — breaks `JSON.stringify`** — every other `bigint` column in the schema uses `mode: 'number'` (confirmed); the JS `bigint` primitive throws `TypeError: Do not know how to serialize a BigInt` when passed to `JSON.stringify`; any endpoint that returns a campaign object will crash at runtime | `packages/shared/src/db/schema.ts` (planned airdrop_campaigns table) | Use `bigint('amount_lamports', { mode: 'number' })` to match all existing bigint columns in the schema |
| A9 | **`announcements.cta_url` has no scheme validation — XSS risk** — `cta_url` is a plain `text` column with no server-side validation; a `javascript:` URI stored here and rendered as a tappable link in the mobile announcement banner executes arbitrary JS in the WebView context | `apps/server/src/routes/v1/admin/announcements.ts` (planned) | Validate `cta_url` at `POST/PATCH /admin/announcements`: reject any scheme other than `https://` or `http://` with 400 `INVALID_CTA_URL` |
| 86 | **No index on `conversations.last_message_at`** — `GET /v1/conversations` orders by `last_message_at DESC`; after filtering by `user_a_id`/`user_b_id` PostgreSQL sorts the filtered rows in memory; as a user's conversation volume grows beyond the 50-row LIMIT's coverage window, the sort degrades without an index | `packages/shared/src/db/schema.ts` | Add composite indexes `(user_a_id, last_message_at DESC)` and `(user_b_id, last_message_at DESC)` so the planner can use an index scan + limit without a sort step |
| A10 | **Airdrop batch building is not idempotent — concurrent requests corrupt batch state** — two simultaneous `POST /admin/airdrops/:id/batches` calls both see `status = 'approved'`, independently build different unsigned transactions for batch 0, and return different `unsigned_tx_base64` values; only one gets signed but both may be recorded | `apps/server/src/routes/v1/admin/airdrops.ts` (planned) | Use `SELECT FOR UPDATE` on the campaign row before building the transaction; also add a unique constraint on `(campaign_id, batch_index)` in `airdrop_transactions` to detect duplicate confirmations at the DB layer |
| A11 | **Eligible airdrop wallets are not snapshotted at approval time** — the wallet list is computed live at execution time; a campaign approved on day 1 and executed on day 5 sends to a different set than what was approved (new signups, eligibility changes); the approver cannot know who will actually receive tokens | `apps/server/src/routes/v1/admin/airdrops.ts` (planned) | At approval time, INSERT eligible wallet IDs into a new `airdrop_eligible_wallets` table; batch building reads from this snapshot, not from the live user table |
| A12 | **No `last_active_at` on users — "active users" metric cannot be computed** — `GET /admin/metrics` is planned to return "active users (7d/30d)" but there is no `last_active_at` timestamp on the `users` table and no session/activity log; the metric cannot be implemented | `packages/shared/src/db/schema.ts`, `apps/server/src/plugins/auth.ts` | Add `last_active_at timestamp` to `users`; update lazily in the `authenticate` decorator only when the current value is null or older than 1 hour to avoid write amplification |
| 87 | **No index on `reports.created_at`** — `GET /v1/admin/reports` (unfiltered) orders by `created_at DESC` with no covering index; as the report backlog grows the planner falls back to a sequential scan + sort | `packages/shared/src/db/schema.ts` | Add `index('reports_created_at_idx').on(t.created_at)` |
| A13 | **Featured gig endpoint has no status guard** — `POST /admin/featured-gigs` does not validate the target gig's status; a draft, cancelled, or expired gig can be featured, placing a dead link on the home screen hero section | `apps/server/src/routes/v1/admin/featured-gigs.ts` (planned) | Guard: `gig.status` must be `'open'` (or `'accepted'` per team decision) before inserting into `featured_gigs`; return 400 otherwise |
| ~~A14~~ | ~~**`GET /v1/gigs/:gigId/dispute/thread` returns 404 for both "gig not found" and "no thread yet"**~~ | ~~resolved~~ | Fixed: returns `200 { thread: null }` when no thread exists yet; 404 only for missing gig/offer. Applied to both gig and exchange thread routes. |
| A15 | **Demoted dispute resolvers leave dispute threads permanently assigned to them** — when a `dispute_resolver` is demoted, their open `dispute_threads` rows still have `assigned_to = their user id`; they can no longer access those threads but a `super_admin` must manually find and reassign each one | `apps/server/src/routes/v1/admin/index.ts` | In `PATCH /admin/users/:id/role`, when the new role is not an admin role, set `assigned_to = NULL` on all open `dispute_threads` where `assigned_to = target user id`; run in the same transaction as the role update |
| 25 | No full-text search on gig title/description — `GET /v1/gigs` supports location/category/price but no keyword search | `routes/v1/gigs/index.ts`, `db/schema.ts` | Add `q` query param; use Postgres `tsvector` + GIN index or `ILIKE` for MVP |
| 26 | Cloudinary upload signature doesn't constrain file size or type — server signs anything | `lib/cloudinary.ts` | Add `max_file_size` and `allowed_formats` to the signature params per upload type |
| 27 | Admin route response types not in shared contracts — a future admin web panel would need to type responses manually | `packages/shared/src/api/contracts/` | Add `AdminContract` to `@tenda/shared` when an admin panel is built |
| 64 | **`batchExpireGigs` runs in the request path** — on two pods the batch runs twice per minute; on a high-traffic instance it adds latency to every `GET /v1/gigs` response | `lib/gigs.ts` | Move to a dedicated cron job or pg_cron so it runs once regardless of pod count |
| 72 | **Cloudinary upload signature expires 10 minutes after generation** — `generateUploadSignature` bakes in the server timestamp; if the mobile client queues the upload (slow network, background processing) and more than 10 minutes pass, Cloudinary rejects the upload with a 401 | `lib/cloudinary.ts`, `apps/mobile/lib/upload.ts` | Fetch a fresh signature immediately before each `fetch(…/auto/upload)` call rather than caching it; or pass `eager_async=true` with a webhook |
| ~~80~~ | ~~**CoinGecko rate-limit exposure from per-device polling**~~ | ✅ Added `GET /v1/platform/exchange-rates` server proxy with 5-min in-process cache + stale fallback on CoinGecko failure; `exchange-rate.store.ts` now calls `api.platform.exchangeRates()` — CoinGecko quota is O(1) per server instead of O(devices) |
| 82 | **No dispute bond** — either party can open a dispute at zero cost, enabling frivolous disputes; combined with the single-admin model there is no deterrent | `tenda-escrow/…/dispute_gig.rs`, `tenda-escrow/…/state/escrow.rs` | Add `dispute_bond` field to `GigEscrow`; require initiating party to deposit a small bond (e.g. 10 000 lamports) on `dispute_gig`; transfer bond to the other party on `resolve_dispute` if initiator loses |
| 84 | **Expo push proxy is a third-party dependency** — all app notifications route through Expo's push service; if it goes down, all notifications (gig events, chat messages, gig alerts) fail silently with no fallback | `apps/server/src/lib/notifications.ts` (planned) | Migrate to direct FCM (Android) + APNs (iOS) when production traffic justifies it; requires Firebase project, APNs key from EAS, and updating `notifications.ts` to call FCM/APNs directly instead of Expo's HTTP API |
| 85 | **No attachment support in chat** — users cannot send images or files in conversations; likely to cause friction when workers need to share clarification photos or posters need to show delivery locations | `apps/mobile/app/chat/[userId].tsx` (planned), `apps/server/src/routes/v1/conversations/_id/messages.ts` (planned) | Add optional `attachment_url` + `attachment_type` fields to `messages` table; reuse existing `FilePicker` + `uploadToCloudinary` on mobile; render inline image or file link in `MessageBubble` |
| G3 | **`withdraw-earnings` builds a transaction without validating the requested amount against the user's on-chain balance** — the route validates only `amount_lamports > 0`; a user requesting more lamports than their `airdrop_sol` balance will get a valid unsigned transaction, pay the Solana tx fee, and have the on-chain instruction fail with `InsufficientFunds`; UX is poor and the tx fee is wasted | `apps/server/src/routes/v1/blockchain/withdraw-earnings.ts` | Return 400 `INSUFFICIENT_BALANCE` without building the transaction; fetch the user's `UserAccount` PDA or maintain a server-side balance mirror so the check is fast |
| ~~G4~~ | ~~`getSignatureStatus` in the transaction status route missing `searchTransactionHistory: true`~~ | ✅ Added `{ searchTransactionHistory: true }` to `getSignatureStatus` call | — `connection.getSignatureStatus(signature)` at line 28 in `transaction.ts` only checks recent slots; any transaction older than ~256 slots (a few minutes on mainnet) will return `null`, causing the API to report `"not_found"` for a successfully confirmed transaction; mobile clients may prompt the user to retry, causing double submission | `apps/server/src/routes/v1/blockchain/transaction.ts` | Pass `await connection.getSignatureStatus(signature, { searchTransactionHistory: true })` |
| ~~G5~~ | ~~`sendPush` uses `console.error`/`console.warn` instead of the structured Fastify logger~~ | ✅ Added `PushLogger` interface parameter to `sendPush`; all 9 callers in `notifications.ts` now pass `fastify.log` | — log lines (lines 62, 72, 79 in `push.ts`) bypass Pino's structured output; in production these appear as unstructured text outside the JSON log stream, making them invisible to log aggregators that parse structured fields like `level`, `time`, `msg` | `apps/server/src/lib/push.ts` | Pass a Pino `Logger` instance (or a `{ log }` context) into `sendPush()`; replace `console.error/warn` with `log.error`/`log.warn` using structured object arguments |
| ~~G6~~ | ~~`sendPush` `fetch` call has no timeout~~ | ✅ Added `signal: AbortSignal.timeout(15_000)` to the Expo push API `fetch` call | — if the Expo Push API is slow or unresponsive, `fetch(EXPO_PUSH_URL)` in `push.ts` awaits indefinitely, blocking the Node.js event loop for every pending push batch and starving other async operations on the same tick | `apps/server/src/lib/push.ts` | Wrap the fetch with `AbortSignal.timeout(15_000)`: `fetch(url, { method, headers, body, signal: AbortSignal.timeout(15_000) })` |
| ~~F6~~ | ~~Message read-receipt update is fire-and-forget with silenced errors~~ | ✅ Awaited the UPDATE so the badge clears reliably before response returns; comment documents a future `POST /v1/conversations/:id/read` explicit read-receipt endpoint as the scalable upgrade path | — `GET /v1/conversations/:id/messages` marks unread messages as read via `void fastify.db.update(...).catch(() => {})` (lines 85–95); if the UPDATE fails (DB overload, transaction deadlock), the error is swallowed and the unread badge for that conversation stays stale on the server permanently; users see a persistent unread badge even after reading the messages | `apps/server/src/routes/v1/conversations/_id/messages/index.ts` | Await the update (or run it in the same implicit transaction as the SELECT); at minimum log errors instead of swallowing them: `.catch((err) => fastify.log.warn({ conversationId: id }, 'read-receipt update failed', err))` |
| E4 | **Chat polling runs at a fixed interval per open screen** — `apps/mobile/app/chat/[userId].tsx` uses `setTimeout` polling (3–10 s); each user with the chat screen open makes 6–20 requests/min; with 1 000 concurrent chat users that is 6 000–20 000 req/min on the messages endpoint alone, with no benefit over a WebSocket connection | `apps/mobile/app/chat/[userId].tsx`, `apps/server/src/routes/v1/conversations/_id/messages/` | Replace polling with a WebSocket or Server-Sent Events channel (Fastify `@fastify/websocket` is already a common dep); fall back to exponential-backoff long-poll only when WS is unavailable |
| ~~E5~~ | ~~**JWT `country` field is stale for the full 7-day token lifetime**~~ | ✅ `POST /v1/gigs` now fetches the poster's current `country` from DB at request time (`SELECT country FROM users WHERE id = poster_id`); `posterCountry` replaces both `request.user.country` uses (remote-gig country fallback and `cross_border` flag) — JWT `country` no longer drives any server-side logic in the create path | `apps/server/src/routes/v1/auth/index.ts`, `apps/server/src/plugins/auth.ts` | Remove `country` from the JWT payload; resolve it from the DB at request time (already cached in `users` table via `statusCache` miss or similar); or issue a token refresh on profile update |
| ~~E6~~ | ~~`replayAll` fires all pending-sync items simultaneously~~ | ✅ Already sequential — `replayAll` uses `for...of` with `await` inside the loop; each item is processed one at a time | — `pending-sync.store.ts` maps over the queue and calls every API endpoint in parallel with `Promise.all`; if the queue grew to 10 items (e.g. offline publish + accept + approve across gigs), all 10 go out at once, potentially triggering on-chain race conditions or rate-limit rejections | `apps/mobile/stores/pending-sync.store.ts` | Process items sequentially (`for…of` with `await`) or group by `gigId` and process groups serially; retry failed items individually without aborting the whole queue |
| ~~D1~~ | ~~Pending-sync queue grows unbounded after max retries~~ | ✅ Dead-letter queue pattern: exhausted items move to `failed[]` instead of being removed; `FailedSyncPanel` component shows a warning banner on the Wallet tab with per-item Retry / Dismiss; Wallet tab badge reflects `failed.length`; signatures preserved for manual recovery | — items that fail `MAX_RETRY_COUNT` times are skipped but never removed; every subsequent `replayAll()` re-attempts them forever, accumulating stale entries in SecureStore | `apps/mobile/stores/pending-sync.store.ts` | Remove the entry (with a log warning) once `retryCount >= MAX_RETRY_COUNT` instead of leaving it in the queue |
| ~~D2~~ | ~~`verifyTransactionOnChain` makes two unbounded RPC calls per request, no cache~~ | ✅ Added `_sigCache` Map with 60 s TTL; cache hit skips all RPC calls; entries pruned lazily on access | — retried requests (client timeout, network loss) re-verify the same signature each time; under retry storms or bot traffic the RPC provider rate-limit is exhausted, breaking all blockchain operations globally | `apps/server/src/lib/solana.ts` | Add a short-lived (30–60 s) in-process cache keyed by signature so repeated calls for the same signature skip the RPC round-trip |
| ~~D4~~ | ~~**Mobile signs transactions without validating program ID or discriminator**~~ | ✅ IDL moved to `packages/shared/src/idl/` (single source of truth); `discriminatorFor(name)` and `InstructionName` type exported from `@tenda/shared/idl`; `validateTransaction(tx, expectedInstruction: InstructionName)` added to `wallet/index.ts`; `startBlockchainFlow` typed with `InstructionName`; called before signing on all 8 paths in `gig/[id]/index.tsx` (publish, accept, dual-tx accept, approve, cancel, refund, dispute, submit); `pnpm sync:idl` script propagates IDL after every `anchor build` |
| ~~D5~~ | ~~`fee_bps` has no upper-bound validation on platform-config update~~ | ✅ Already validated — `fee_bps` rejects values outside 0–10 000 with a 400 error (added alongside `seeker_fee_bps` in F5) | — an admin (or attacker with admin credentials) setting `fee_bps = 100_000` would compute a platform fee larger than the payment itself, breaking all publish/approve/resolve flows | `apps/server/src/routes/v1/admin/` (platform-config PATCH) | Reject `fee_bps` outside a reasonable range (e.g. 0–10 000, i.e. 0–100%) with a 400 validation error |
| ~~D6~~ | ~~`batchExpireGigs` has no observability~~ | ✅ `lib/gigs.ts`: both UPDATEs now use `.returning({ id: gigs.id })`; success logs count via `log.info`; wrapped in try/catch with `log.error` on failure; caller in `gigs/index.ts` now passes `fastify.log` as second arg | — the UPDATE runs silently with no log of how many gigs were expired or whether the query failed; a broken expiry goes undetected until users notice stale open gigs | `apps/server/src/lib/gigs.ts` | Log the count of rows updated on success; catch and log errors from the UPDATE; consider emitting a metric |

---

## Resolved

| # | Issue | Resolved in |
|---|-------|-------------|
| 1 | No rate limiting | Added global 100 req/min + 10 req/min on `/auth/wallet` |
| 2 | `winner` field unvalidated in `resolve.ts` | Validated against `['poster', 'worker', 'split']` |
| 3 | Suspended users keep JWT valid up to 7 days | 60s in-process status cache added to `authenticate` |
| 4 | No pagination `limit` cap | Clamped to `MAX_PAGINATION_LIMIT = 100` on all paginated endpoints |
| 5 | Grace period not enforced in worker submission window | `submit.ts` now allows submission within `completion_deadline + grace_period_seconds`; returns `GRACE_PERIOD_EXPIRED` after |
| 6 | `reputation_score` race condition under concurrent reviews | Single atomic `UPDATE … SET = (SELECT ROUND(AVG …))` |
| 7 | No admin routes | Added `GET /disputes`, `PATCH /users/:id/status`, `GET/PATCH /platform-config` |
| 8 | Role changes don't propagate until JWT expiry | Partially mitigated — cache-miss forces DB status re-check; full fix tracked in #15 |
| 9 | No health check endpoint | Added `GET /health → { status, uptime }` |
| 10 | Cloudinary URL not validated on receipt | `isCloudinaryUrl()` applied to `avatar_url` and proof URLs |
| 11 | Lazy expiry only ran on single-record GET | `batchExpireGigs()` with 60s throttle called at top of `GET /v1/gigs` |
| 12 | Full gig financial history publicly visible per user | Optional auth on `GET /v1/users/:id/gigs`; non-owners see public statuses only |
| 13 | `any` used in `lib/gigs.ts` and `lib/platform.ts` | Replaced with `AppDatabase = PostgresJsDatabase<typeof schema>` |
| 14 | Rate limit keys on proxy IP, not client IP | Set `trustProxy: true` on the Fastify instance in `server.ts` |
| 28 | TOCTOU race on gig accept/approve/dispute/submit — UPDATE had no status guard in WHERE clause | Added `eq(gigs.status, expectedStatus)` to UPDATE WHERE in all four routes; 0 rows returned → 409 |
| 15 | Role not reflected until JWT expiry | Acceptable — promoted users re-login to get updated role in JWT |
| 16 | CORS wide open (`origin: true`) | `CORS_ORIGIN` env var; falls back to `true` in dev |
| 18 | Duplicate on-chain signatures writable to `gig_transactions` | `uniqueIndex` on `gig_transactions.signature`; 23505 → 409 `DUPLICATE_SIGNATURE` in publish, approve, resolve |
| 19 | No API to promote user to admin | Added `PATCH /v1/admin/users/:id/role`; self-demotion guarded; role change logged |
| 29 | `createEscrowInstruction` used env-var fee instead of DB fee | `feeBps` param added; caller passes `getPlatformConfig().fee_bps` |
| 30 | `createEscrowInstruction` built a plain `SystemProgram.transfer` — GigEscrow account never initialised | Replaced with `buildCreateGigEscrowInstruction` using real Anchor discriminator + borsh-encoded args |
| 31 | Graceful shutdown commented out | Uncommented `server.close()` on SIGTERM/SIGINT |
| 40 | No server endpoint to build `approve_completion` unsigned tx | Added `POST /v1/blockchain/approve-escrow`; `accept.ts` + `submit.ts` now require on-chain signature |
| 41 | No server endpoint to build `cancel_refund` unsigned tx | Added `POST /v1/blockchain/cancel-escrow`; mobile calls `DELETE /v1/gigs/:id` with signature |
| 42 | No blockchain endpoints for `accept_gig`, `submit_proof`, `refund_expired` | Added `POST /v1/blockchain/accept-gig`, `submit-proof`, `refund-expired`; `POST /v1/gigs/:id/refund` records expired_refund transaction |
| 43 | **On-chain signature not tied to gig or action** | `lib/solana.ts`: exported 6 discriminator constants; `verifyTransactionOnChain` now accepts `expectedDiscriminator?` and fetches the full tx to assert programId + discriminator match. Each route (`publish`, `accept`, `submit`, `approve`, `refund`) passes its discriminator. |
| 44 | **No unique constraint on `(gig_id, reviewer_id)` in reviews** | `db/schema.ts:194` already has `uniqueIndex('reviews_gig_reviewer_unique')`; `review.ts:107–122` already catches 23505 → 409. No code change needed. |
| 45 | **Draft gig with a past `accept_deadline` can be published** | `publish.ts`: added pre-publish check — rejects with `ACCEPT_DEADLINE_PASSED` if `accept_deadline <= now()`. |
| 46 | **`checkAndExpireGig` not called before refund-expired routes** | `gigs/_id/refund.ts` and `blockchain/refund-expired.ts`: both now call `checkAndExpireGig()` before the status guard, so a poster hitting either endpoint directly gets the correct `expired` status. |
| 47 | **`recentBlockhash` not set on any unsigned transaction** | All 6 `buildX` functions in `lib/solana.ts` are now `async` and set `transaction.recentBlockhash` from `getLatestBlockhash('confirmed')` before serializing. Blockchain route handlers updated to `await` the calls. |
| 48 | **Proof `type` field not validated as enum before DB insert** | `submit.ts`: validates each proof's `type` against `['image', 'video', 'document']` before the URL check; returns 400 `VALIDATION_ERROR` for any other value. |
| 49 | **`submitProof`/`acceptGig` store merge corrupts `GigDetail`** | `gigs.store.ts`: both `acceptGig` and `submitProof` now call `fetchGigDetail(id)` after the API call instead of spreading the raw response over `selectedGig`. |
| 50 | **Concurrent blockchain flows overwrite each other's state** | `GigCTABar.tsx`: added `txInProgress: boolean` prop — when true, renders a "Transaction in progress" warning instead of any CTA. `gig/[id].tsx`: passes `txInProgress={pendingSignature !== null}`. |
| 51 | **Cloudinary URL validation uses substring match** — `res.cloudinary.com.evil.com` passed a naive `startsWith` check | `shared/src/utils/validation.ts`: replaced with `new URL(url).hostname === 'res.cloudinary.com'` |
| 52 | **No rate limit on `/dispute`, `/review`, `/refund`** — only global 100/min applied | Added `config.rateLimit`: dispute 5/min, review 10/min, refund 3/min |
| 53 | **Pending-sync queue persists across logout** | `pending-sync.store.ts`: added `clear()` action; `auth.store.ts` logout calls `clear()` before `clearAuthStorage()` |
| 54 | **No SOL balance check before publish** | `gig/[id].tsx:handlePublish`: pre-flight `getBalance` check; shows Alert if insufficient; requires new `GET /v1/platform/config` endpoint |
| 55 | **`computePlatformFee` integer overflow** — `bigint × number` throws TypeError | Moved to `shared/src/utils/fees.ts` using BigInt arithmetic; all server callers updated; `SOLANA_TX_FEE_LAMPORTS` added to `shared/src/constants/solana.ts` |
| 56 | **Wallet screen made O(n) API calls** — one per gig | Added `GET /v1/users/:id/transactions` (single JOIN); wallet screen now makes one call |
| 57 | **`poster` had `isSigner: false` in `refund_expired` instruction** | `lib/solana.ts:buildRefundExpiredInstruction`: changed to `isSigner: true` |
| 67 | **Auth upsert TOCTOU race → 500 on concurrent first-logins** | `auth/index.ts`: replaced SELECT + conditional INSERT with `INSERT … ON CONFLICT DO UPDATE … RETURNING` |
| 68 | **Auth message had no nonce/expiry — replayable indefinitely** | Mobile: timestamp appended to message; server: validates timestamp within ±5 min / 30 s window |
| 69 | **Submit-proof had no pending-sync retry path** | `pending-sync.store.ts`: added `'submit'` action with `proofs` field; `gig/[id].tsx:handleProofsReady`: adds to pending-sync with signature + proofs |
| 70 | **`loadSession` cleared JWT on any error including network failures** | `api/client.ts`: exported `ApiClientError`; `auth.store.ts`: only clears storage on 401/403, preserves credentials on network errors |
| B1 | **`earned_sol` tracked in `UserAccount` but `approve_completion` paid worker wallet directly** — `withdraw_earnings` tried to deduct from PDA lamports that were never deposited; workers could not withdraw gig earnings | `tenda-escrow/…/state/user.rs`, `tenda-escrow/…/withdraw_earnings.rs` | `earned_sol` is now tracking-only (lifetime counter); `withdrawable_balance()` returns only `airdrop_sol`; `deduct_withdrawal` only touches `airdrop_sol`; `withdraw_earnings` is exclusively for the airdrop subsidy |
| B2 | **Platform fee refunded to poster on dispute win** — admin was financially incentivised to avoid ruling in poster's favour (fee earned in 2 of 3 outcomes) | `tenda-escrow/…/resolve_dispute.rs` | Platform keeps fee in all three outcomes (`Poster`, `Worker`, `Split`); poster refunded payment only; comment documents rationale |
| B3 | **`GIG_ESCROW_SIZE` constant was wrong and unused** — comment claimed "8×5 timestamps" (40 bytes) but actual timestamp fields total 61 bytes; constant not used for account `space` | `tenda-escrow/…/constants.rs` | Deleted `GIG_ESCROW_SIZE`; `create_gig_escrow` already used the correct `GigEscrow::LEN` |
| B4 | **`GigEscrow::LEN` comment stale after UUID hyphen-strip change** — still said `(4 + 36)` for `gig_id` after `MAX_GIG_ID_LEN` was reduced to 32 | `tenda-escrow/…/state/escrow.rs` | Updated to `(4 + 32)` matching the compact UUID format |
| B5 | **`total_gigs` and `total_volume` in `PlatformState` used `checked_add`** — overflow (astronomically unlikely but possible) would permanently brick `create_gig_escrow` and `approve_completion` | `tenda-escrow/…/create_gig_escrow.rs`, `tenda-escrow/…/approve_completion.rs` | Changed both to `saturating_add`; stats counters have no security significance so saturation is correct |
| B6 | **Odd-lamport split behaviour undocumented** — `payment_amount / 2` integer division leaves 1 lamport in escrow on odd amounts; silently goes to poster via `close = poster` | `tenda-escrow/…/resolve_dispute.rs` | Added comment documenting the rounding; poster receives `half + 1` — accepted behaviour |
| A1 | **`resolve.ts` accepted any confirmed Solana tx** — no discriminator passed to `verifyTransactionOnChain` | Added `DISCRIMINATOR_RESOLVE_DISPUTE` to `lib/solana.ts`; now passed alongside all other routes |
| A2 | **`resolve.ts` UPDATE had no status guard** — concurrent resolves could insert duplicate transaction records | Added `eq(gigs.status, 'disputed')` to WHERE; null result returns 409 |
| A3 | **`publish.ts` UPDATE had no status guard** — inconsistent with all other state-transition routes | Added `eq(gigs.status, 'draft')` to WHERE; null result returns 409 |
| A4 | **Auth message `Chain:` line not validated** — devnet-signed messages accepted by production server | Added `solanaChainId(network)` helper to `shared/constants/solana.ts`; server verifies chain; mobile uses same helper |
| A5 | **lat/lng not range-validated on profile update or gig creation** | `isValidLatitude`/`isValidLongitude` added to `@tenda/shared`; applied in `users/_id/index.ts` and `gigs/index.ts` |
| A6 | **Network error in `loadSession` left in-memory auth state blank** — false login screen shown | Credentials read into outer-scope `let` vars; committed to state in network-error branch |
| A7 | **`review.ts` + `dispute.ts` used inline 23505 check** instead of `isPostgresUniqueViolation()` helper | Replaced with helper in both files |
| A8 | **Review comment length not validated before DB insert** — DB threw cryptic 500 on >1000 chars | Added `MAX_REVIEW_COMMENT_LENGTH` constant to shared; validated in `review.ts` before insert |
| A9 | **Public `GET /v1/platform/config` had no explicit rate limit** | Added `config: { rateLimit: { max: 30, timeWindow: '1 minute' } }` |
| A10 | **statusCache eviction ran O(n) on every cache miss** | Replaced inline sweep with `setInterval` every 5 min (`.unref()` so it doesn't block test teardown) |
| A11 | **min/max payment filter in GET /gigs not validated** — negative values or min > max silently returned wrong results | Validates both are non-negative integers and min ≤ max; haversine SQL refactored to use typed numeric locals |
| A12 | **MWA error helpers used `any` type** | Introduced `MwaError` interface + `isMwaError` type guard; `catch` block changed to `unknown` |
| A13 | **`replayAll` overwrote in-memory queue from disk** — unsaved in-memory items dropped on merge | Merges disk items into in-memory queue using Set deduplication by id |
| 17 | **`statusCache` grew indefinitely** — no eviction of expired entries | `plugins/auth.ts`: added eviction sweep on every cache miss |
| 20 | **`GET /admin/platform-config` returned empty body if table unseeded** | Returns 404 with clear error message |
| 21 | **No upper bound on `grace_period_seconds`** — could be set to an arbitrary value | Capped at 30 days (2,592,000 s) with validation error on exceeded value |
| 23 | **No readiness probe** — `GET /health` skips DB; k8s can't detect broken DB connection | Added `GET /health/ready` running `SELECT 1`; returns 503 on failure. Both health routes rate-limited (60/min liveness, 30/min readiness) |
| 32 | **PATCH draft gig skipped deadline re-validation** — past `accept_deadline` accepted | `routes/v1/gigs/_id/index.ts`: calls `validateGigDeadlines` with effective post-PATCH values |
| 33 | **`requireRole` called `jwtVerify()` redundantly** — token already verified by `authenticate` | `lib/guards.ts`: removed redundant `jwtVerify()` call |
| 34 | **`GET /gigs/:id` fetched poster and worker sequentially** | Wrapped poster + worker + proofs + dispute in a single `Promise.all` |
| 35 | **`pino-pretty` and `level: 'debug'` hardcoded in production** | `server.ts`: conditionally applies pino-pretty and `debug` level only when `NODE_ENV !== 'production'` |
| 36 | **`getConnection()` created a new Solana `Connection` on every call** | `lib/solana.ts`: module-level singleton; `getConnection()` returns cached instance |
| 37 | **`SOLANA_PROGRAM_ID` had a placeholder fallback** — wrong program ID used silently if env missing | Moved to `REQUIRED_ENV_VARS`; startup validates base58 format |
| 38 | **`GET /blockchain/transaction/:signature` was unauthenticated** — any caller could drain RPC quota | Added `preHandler: [fastify.authenticate]` |
| 58 | **`startBlockchainFlow` could leave state partially set on signing error** | Wrapped in try/catch; clears `pendingSignature`, `pendingAction`, `pendingSyncId` in catch before re-throwing |
| 59 | **`mwaAuthToken` null tap was a silent no-op** | Shows toast "Wallet not connected — please reconnect and try again" before returning |
| 60 | **Proof upload failure silently lost all selected files** | Upload loop returns on first failure; per-file toast names the failing file; `proofFiles` retained for retry |
| 61 | **Dispute/review form state persisted across sheet opens** | `handleClose` resets `disputeReason`, `reviewScore`, and `reviewComment` before calling `onClose()` |
| 62 | **No confirm modal before "Claim Refund"** — blockchain tx fired immediately | Added confirm modal (same pattern as cancel/approve) with `ActiveSheet = 'refund'` |
| 63 | **`accept_deadline` displayed without time when < 24 h away** | `lib/gig-display.ts:formatDeadline`: uses `toLocaleString` with hour/minute when deadline < 24 h |
| 65 | **Review score range not enforced server-side** — score 0 or 6 could corrupt AVG | `review.ts`: `isValidReviewScore` helper rejects values outside 1–5 with 400 |
| 66 | **Gig UUID not asserted before borsh encoding** — multi-byte chars silently malformed instruction | `lib/solana.ts:encodeBorshCreateGigArgs`: UUID regex assertion throws on invalid input |
| 71 | **`solToNgn` division-by-zero if CoinGecko returns 0** — rendered `Infinity`/`NaN` | `lib/currency.ts`: guards `solToNgn <= 0`; returns `naira: 0` safely |
| 73 | **No index on `gigs.accept_deadline`** — batch expiry degraded to sequential scan | `db/schema.ts`: added `index('gigs_accept_deadline_idx').on(t.accept_deadline)` |
| 74 | **Admin could suspend another admin** — no cross-admin guard on status update | Fetches target role before update; returns 403 if target is admin |
| 75 | **No audit log for user suspension/reinstatement** | `fastify.log.info({ adminId, targetId, newStatus }, 'User status changed')` added after successful update |
| 76 | **`PATCH /admin/platform-config` silently returned empty body when config row missing** | Added `if (!updated)` check returning 404 |
| 77 | **No server-side limit on proof count in `POST /gigs/:id/submit`** | Returns 400 if `proofs.length > 10` |
| 78 | **Haversine filter parameters not range-validated** — `lat=999` silently passed to Postgres | Validates `lat ∈ [−90, 90]`, `lng ∈ [−180, 180]`, `radius_km ∈ (0, 20 000]`; returns 400 on invalid |
| 79 | **`uploadToCloudinary` had no timeout** — hangs indefinitely on slow network/outage | Wrapped in `AbortController` with 120 s timeout; `AbortError` surfaced as user-friendly toast |
| C1 | **Dead push tokens never cleaned up** — `DeviceNotRegistered` errors logged but tokens left in `device_tokens`; wasted API calls on every subsequent push | `sendPush` now returns invalid token strings; `notifications.ts` calls `removeStaleTokens()` to delete them from DB after every send |
| C2 | **Unread badge stale until next `fetchConversations`** — reading a conversation didn't clear local unread count until screen blur/focus cycle | `chat.store.ts:fetchMessages` zeroes `unread_count` for the opened conversation and recalculates total `unread` immediately on load |
| C3 | **Send-to-closed-conversation race** — status check and INSERT were separate DB operations; a concurrent `POST /close` could land between them | Transaction now opens with `SELECT … FOR UPDATE` on the conversation row; status re-checked inside the lock before INSERT |
| C4 | **Device token `user_id` overwritten on conflict** — `onConflictDoUpdate` reassigned ownership to any authenticated caller who knew the token | Conflict handler now only updates `updated_at`; `user_id` is never changed after initial registration |
| D3 | **Dispute endpoint verifies on-chain signature before checking for an existing dispute** — a duplicate request wastes user gas (second on-chain tx fee) before hitting the DB unique constraint | `dispute.ts`: pre-check `SELECT id FROM disputes WHERE gig_id = id` before `verifyTransactionOnChain`; unique-violation catch still handles the concurrent-request race |
| D7 | **submit.ts deadline check silently skipped when `accepted_at` is NULL** — an accepted gig with a missing `accepted_at` bypassed the submission window entirely; root cause is data corruption but the bypass made it undetectable | `submit.ts`: explicit null guard returns 500 with error log before the deadline calculation; deadline check now runs unconditionally |
| D4 | **Mobile signs transactions without validating program ID or discriminator** — a MITM or compromised server could substitute any transaction payload for the wallet to sign | IDL moved to `@tenda/shared/idl`; `validateTransaction(tx, expectedInstruction: InstructionName)` in `wallet/index.ts` asserts programId + 8-byte Anchor discriminator before signing; called on all 8 tx paths; `pnpm sync:idl` keeps IDL current after `anchor build` |
| N5 | **`accept_gig` missing from `gigTransactionTypeEnum`** — accepting a gig never inserted a `gig_transactions` row | Added `accept_gig` to Drizzle enum + TypeScript union; `accept/index.ts` now inserts the record inside the status-guarded transaction |
| N6 | **Close-conversation returned `other_user.id: ''`** — both early-return paths used a hardcoded empty string | Computed `otherId` from `user_a_id`/`user_b_id` and used it in both return paths |
| N7 | **Notifications plugin had duplicate token-fetch logic** — `tokensFor` helper existed but `gig.created` fan-out duplicated its query inline | Switched `tokensFor` to `inArray`; `gig.created` now calls `tokensFor(subscriberIds)` instead of repeating the query |
| N8 | **`GET /v1/users/:id/transactions` was unbounded** — no pagination; O(N) response for prolific users | Added `limit`/`offset` (default 20, max `MAX_PAGINATION_LIMIT`) and a parallel COUNT; returns `PaginatedResponse<UserTransaction>`; contract + mobile client updated |
| N9 | **Public `GET /v1/users/:id` exposed `status` field** — suspension state visible to all callers | Removed `status` from the GET column projection; added `'status'` to `PublicUser` omit set |
| N10 | **`GET /v1/gigs/:id` made two sequential user queries** — poster fetched first, then worker if present | Merged into a single `inArray` query + `Map` lookup; same 4-way `Promise.all` structure |
| N11 | **`blockchain/accept-gig.ts` swallowed non-AppErrors** — catch block re-wrapped everything as 500 | Added `if (err instanceof AppError) throw err` before the generic 500 wrap |
| N12 | **Auth re-login with no country cleared stored country** — `onConflictDoUpdate` always set `country: null` when absent | Changed set to `...(country ? { country } : {})` — country preserved if not provided |
| N13 | **`before_id` cursor not scoped to current conversation** — wrong pagination when UUID from another conv passed | Added `eq(messages.conversation_id, id)` to cursor SELECT |
| N14 | **Wallet "Earned" overstated by platform fee** — `amount_lamports` is gross; worker net is `payment - fee` | Changed reduce to subtract `platform_fee_lamports` |
| N15 | **Cancel/refund transactions recorded wrong fee for seeker gigs** — used `fee_bps` instead of `seeker_fee_bps` | Added `effectiveFeeBps = request.user.is_seeker ? config.seeker_fee_bps : config.fee_bps` in gig cancel, expired-refund, and exchange cancel routes |
| N16 | **`GET /v1/users/:id/transactions` ordered oldest-first** — wallet history showed oldest entries at top | Changed to `desc(gig_transactions.created_at)` |
| N17 | **Admin self-demotion threw HTTP 400 instead of 403** — wrong status code for an authorisation failure | Changed `new AppError(400, …)` → `new AppError(403, …)` |
| N18 | **`GET /v1/admin/blocked-keywords` returned all rows unbounded** — no limit on response size | Added `limit`/`offset` pagination and a COUNT query |
| N19 | **Wallet `TYPE_LABEL` missing `accept_gig` entry** — new transaction type rendered as raw DB key | Added `accept_gig: 'Gig accepted'` to the `TYPE_LABEL` map |
| N20 | **`gigs/_id/publish/index.ts` `create_escrow` record used `fee_bps` for seeker posters** — seeker gigs were published with `seeker_fee_bps` on-chain but the DB transaction recorded a higher `platform_fee_lamports`; cancel/refund calculations based on the recorded escrow total would also be off | Changed to `effectiveFeeBps = request.user.is_seeker ? config.seeker_fee_bps : config.fee_bps` in the publish route (same fix already applied to approve, cancel, and refund) |
| N21 | **`exchange/_id/publish/index.ts` `create_escrow` record used `fee_bps` for seeker sellers** — same issue as N20 but for P2P exchange offers; `computePlatformFee(BigInt(offer.lamports_amount), config.fee_bps)` used standard rate regardless of seller's seeker status | Changed to `effectiveFeeBps = request.user.is_seeker ? config.seeker_fee_bps : config.fee_bps` |
| N22 | **`reports/index.ts` catch block returned `content_id` instead of the report ID** — the `isPostgresUniqueViolation` fallback at line 111 returned `{ id: content_id }` (the UUID of the reported gig/message/user), not the existing report's UUID; callers that stored the returned ID for later follow-up would hold a wrong reference | Added a `SELECT id FROM reports WHERE reporter_id = X AND content_type = Y AND content_id = Z` lookup in the catch block, matching the `!inserted` path above it |
| N23 | **`pending-sync.store.ts` treated all HTTP 409 responses as `DUPLICATE_SIGNATURE`** — any `409` from the server (including `GIG_WRONG_STATUS`) silently removed the pending item from the queue; a gig-status race would permanently drop a valid transaction that needed manual retry | Added `err.error === ErrorCode.DUPLICATE_SIGNATURE` check alongside `err.statusCode === 409`; other 409 codes now fall through to the retry path |
| N24 | **`home.tsx` used `as any` for category filter** — `setFilters({ category: (cat ?? undefined) as any })` bypassed TypeScript's `GigCategory` type check; a stale or invalid category string would reach the API silently | Changed to `(cat ?? undefined) as GigCategory \| undefined` — explicit cast preserves intent while maintaining nominal type safety |

---

# Mobile V2 Redesign — Open Items

> Tracker for v1 deferrals, wireframe deviations, and follow-ups recorded during the Stage 1 + Stage 2 wireframe-to-unistyles translation pass.
> Source-of-truth: `/home/abioye/tenda/Tenda-redesign/WIREFRAME-SPEC-REFERENCE.md`. Working docs: `/home/abioye/tenda/Tenda-redesign/implementation/`.

## 🟠 Significant — visual/functional gaps from wireframe

| # | Issue | File(s) | Why deferred / Fix |
|---|-------|---------|-------------------|
| M1 | **Tab bar BlurView missing** — wireframe specifies `rgba(255,255,255,0.88) light / rgba(22,27,41,0.88) dark + backdrop-filter: blur(20px)`. Currently ships opaque `surface.background` because `expo-blur` is not installed. Visually noticeable on screens where content scrolls under the tab bar | `apps/mobile/app/(tabs)/_layout.tsx` | Install `expo-blur`; wrap `tabBar` background in `<BlurView intensity={20} tint="default">` |
| M2 | **Skeleton shimmer wave missing** — wireframe `.sk` uses `linear-gradient(90deg, --inset 0%, --line 50%, --inset 100%)` with `background-size: 200% 100%` translating across (1.6s ease-in-out). Currently ships opacity pulse — visually less polished than wave shimmer | `apps/mobile/components/ui/Skeleton.tsx` | Install `expo-linear-gradient`; render a sliding lighter band with `withRepeat`/`withTiming` translateX |
| M3 | **ErrorState radial glow simulated as crisp circle** — wireframe `.err-glow` uses `radial-gradient(circle, rgba(203,58,58,0.12), transparent 60%)`. RN can't render radial gradients natively. Currently ships a 280×280 circle at `feedback.danger.surface` opacity 0.5 — crisp edge instead of soft fade | `apps/mobile/components/feedback/ErrorState.tsx` | Use `react-native-svg` `RadialGradient` defs, or `expo-linear-gradient` with mask trick |
| M4 | **Input focus/error 3px halo ring missing** — wireframe `.input.focus` uses `box-shadow: 0 0 0 3px var(--brand-surface)` and `.input.err` uses `--danger-surface`. RN's `boxShadow` style is iOS-only and unreliable on Android. Currently ships border-color flip only | `apps/mobile/components/ui/Input.tsx` | Wrap container in an outer View with conditional bg + 3px padding to simulate the ring (no layout shift if padding is reserved when not focused) |
| M5 | **BottomSheet underlying-screen scale + blur backdrop missing** — spec doc §2.8 specifies `transform: scale(0.96) + opacity 0.55–0.6 + filter: blur(1.5–6px)` on the screen behind the sheet. Currently ships scrim only (`rgba(20,22,30,0.35)`) | `apps/mobile/components/ui/BottomSheet.tsx`, `apps/mobile/app/_layout.tsx` | Lift sheet-open state to the Stack wrapper in `_layout.tsx`; apply scale/opacity to the Stack layout via Reanimated when any sheet opens |
| M6 | **Toast trailing action button not implemented** — wireframe `.toast .ta` shows a 12.5/600 --brand action label trailing the message. Current `showToast(variant, message, duration)` API doesn't accept a handler | `apps/mobile/components/ui/Toast.tsx` | Extend API to `showToast(variant, message, options?: { duration, action?: { label, onPress } })`; render trailing pressable inside the toast row |

## 🟡 Minor — small visual approximations & one-offs

### Primitive-level deferrals

| # | Issue | File(s) | Why deferred / Fix |
|---|-------|---------|-------------------|
| M7 | **Drawer header avatar uses solid accent instead of warm peach gradient** — wireframe `.drawer-av` shows `linear-gradient(135deg, #F5DCC3, #E9B48A)`. My Avatar primitive ships `gradient='accent'` (solid `accent.primary` base) | `apps/mobile/components/ui/Avatar.tsx`, `apps/mobile/components/navigation/DrawerContent.tsx` | Add a `'warm'` gradient preset to Avatar (true 2-stop gradient via `expo-linear-gradient`) |
| M8 | **Drawer header layout intentionally horizontal** — wireframe stacks avatar above name vertically; mine lays them in a row (avatar left, name+handle+badge column right) for compactness in the 300px-wide drawer. Recorded as accepted deviation | `apps/mobile/components/navigation/DrawerContent.tsx` | None (intentional). Revisit if a wider drawer is adopted |
| M9 | **Drawer item list sourced from app nav, not wireframe** — wireframe shows generic mock; mine uses Marketplace (Home / My Gigs / Post Gig) / Money (Trade / Wallet) / Account (Settings) | `apps/mobile/components/navigation/DrawerContent.tsx` | Verify section grouping with stakeholders if a specific wireframe order is required |
| M10 | **Drawer header avatar size = `lg` (48) instead of wireframe 64** — Avatar primitive only supports sm/md/lg/xl (32/44/48/96). 64 isn't a preset | `apps/mobile/components/ui/Avatar.tsx` | Add a numeric `size` override or a new preset (e.g. `'2xl': 64`) |
| M11 | **BottomSheet bg defaults to `surface.background`** — most wireframes use --bg, but modals.html (currency picker) uses `--card`. Caller must pass a style override for that case | `apps/mobile/components/ui/BottomSheet.tsx` | Add `surface?: 'background' \| 'card'` prop if --card variant becomes common |
| M12 | **BottomSheet title fixed at 20/700** — some sheets use 17 (compact options) or 22 (currency picker) | `apps/mobile/components/ui/BottomSheet.tsx` | Add `titleSize?: 'sm' \| 'md' \| 'lg'` prop if needed |
| M13 | **MoneyText currency unit suffix not implemented** — wireframe `.gcc-amt .unit` shows trailing currency label inline (`USD`/`NGN`/`SOL`). Current API doesn't accept a unit prop | `apps/mobile/components/ui/MoneyText.tsx` | Extend `MoneyTextProps` with optional `unitLabel?: string`; render trailing in `--ink-3`, one size step smaller |
| M14 | **MoneyText sans outlier on gig-detail meta** — `.meta-pay-amt` is sans 22/700 (vs canonical mono). Mine renders all amounts in mono. Stage 4 #29 will need a screen-local override for that one spot | `apps/mobile/app/gig/[id]/index.tsx` (Stage 4 #29) | Render the meta-pay-amt as plain `<Text>` instead of `<MoneyText>` |
| M15 | **Avatar 40px size not in primitive enum** — used by `.offer-avatar` (exchange) and `.person-avatar` (gig-detail). Stage 3 #21/#22 will compose these locally | `apps/mobile/components/ui/Avatar.tsx` | Add a numeric `size` override; or extend enum with a `'mid': 40` preset |
| M16 | **Avatar `.avatar-mini` 22px stacking variant not in primitive** — gig card sender thumbnail with --card border + --line ring. Stage 3 #20 GigCardCompact will style locally | `apps/mobile/components/gig/GigCardCompact.tsx` (Stage 3 #20) | Compose at the screen level — this is a screen-specific stacking-circle pattern, not a generic primitive |
| M17 | **Profile `.info-pill` (32h sans) not folded into Badge** — distinct shape (32h vs 24h status-pill). Treated as an "info chip" pattern | `apps/mobile/app/(tabs)/profile.tsx` (Stage 4 #28) | Build a small `InfoPill` screen-local component or extend Badge with `size='lg'` |
| M18 | **Gig-detail `.meta-escrow` (uppercase escrow chip) not folded into Badge** — single instance with 0.10em tracking + uppercase + custom #0D6640 light text | `apps/mobile/app/gig/[id]/index.tsx` (Stage 4 #29) | Inline composition |
| M19 | **`.tag-dotlabel` (gig-detail) uses ok-surface bg with --ink-2 text** — single composition variant (not tonal text); not a generic Badge tone | Stage 4 #29 | Inline composition |
| M20 | **PM-pill (`.pm-pill`) not folded into Badge** — 22h, --inset bg, no dot. Already approximated by `<Badge variant='neutral' size='sm' showDot={false} />` | `apps/mobile/components/exchange/detail/...` (Stage 3 #21) | Should already work via current Badge API — verify in Stage 3 |
| M21 | **Auth `.btn-primary` (R16, F16, heavier shadow) not folded into Button** — single welcome CTA; screen-local override | `apps/mobile/app/(auth)/welcome.tsx` (Stage 4 #25) | Override style at the screen level |
| M22 | **Gig-detail `.cta-*` family (R16) screen-local** — gig-detail is modal-presentation with its own visual language | Stage 4 #29 | Build screen-local CTA bar |
| M23 | **Profile `.disconnect` (1px --danger border) screen-local** — single instance | `apps/mobile/app/(tabs)/profile.tsx` (Stage 4 #28) | Build screen-local Pressable |
| M24 | **Exchange-detail `.btn-overflow` (52×52 --inset square) screen-local** — pure icon button at non-Button radius | Stage 4 #29 | Use `IconButton` primitive with size override |
| M25 | **Profile `.cta` and Update-profile `.btn-primary` font 15.5** — vs canonical 15. 0.5px difference, accepted as visual noise | `apps/mobile/components/ui/Button.tsx` | Add a `font='emphatic'` or `size='lg-bold'` (font 15.5) variant only if user disagrees |
| M26 | **Modal sheet button radius 12 vs primitive 14 at md=48h** — modal sheet CTAs render slightly chunkier than wireframe | `apps/mobile/components/ui/Button.tsx` | Add a `roundness: 'sm' \| 'md'` prop, or scale radius further at md size |
| M27 | **Button press animation = static via Pressable's `pressed` state** — no eased duration. Wireframe spec calls for scale 0.985 + opacity 0.9 over `duration.fast` | `apps/mobile/components/ui/Button.tsx` | Wrap with reanimated for spring-eased press transform |
| M28 | **Toast `'warning'` tone is extrapolated** — wireframe shows `ok/err/info` only; warning derived from feedback color set to align with the rest of the app's tone vocabulary | `apps/mobile/components/ui/Toast.tsx` | Verify with design — keep or remove |
| M29 | **LoadingScreen logo+ring composition removed** — replaced with spec-prescribed simple 14×14 ring spinner. If brand presence on cold-load is desired, needs a separate brand splash | `apps/mobile/components/feedback/LoadingScreen.tsx` | Build a separate `BrandSplash` component if the logo experience matters on cold-load |

### Token / theme amendments resolved during Stage 2 (recorded for traceability)

| # | Issue | File(s) | Status |
|---|-------|---------|--------|
| M30 | **`typography.styles.button.letterSpacing` was +0.1, not -0.15** — wireframe `.btn-primary` consistently uses `letter-spacing: -0.01em` (= -0.15 at 15px). Original spec-doc paraphrase carried over the V1 `+0.1` value | `apps/mobile/theme/tokens.ts` | ✅ Resolved during Stage 2 #15 audit (Stage 1 amendment) |
| M31 | **Dark border alphas were doubled vs wireframe** — `colors.dark.border.{subtle,default,divider}` was `0.08/0.10/0.08` → corrected to `0.04/0.08/0.04` to match `shared.css` dark `--line-subtle: 0.04`, `--line: 0.08` | `apps/mobile/theme/tokens.ts` | ✅ Resolved during Stage 2 #15 audit (Stage 1 amendment) |
| M32 | **`shadows.fab` was `{0,12}/0.20/28`** — corrected to wireframe canonical `{0,10}/0.18/24` (six wireframe primary CTAs share `0 10px 24px rgba(46,91,214,0.18)`) | `apps/mobile/theme/tokens.ts` | ✅ Resolved during Stage 2 #15 audit (Stage 1 amendment) |

### Code hygiene

| # | Issue | File(s) | Why deferred / Fix |
|---|-------|---------|-------------------|
| M33 | **Orphan `components/ui/ErrorState.tsx`** — duplicate ErrorState with `onRetry` API exists; no call sites import it (all use `@/components/feedback/ErrorState`). Left untouched to avoid surprise breakage | `apps/mobile/components/ui/ErrorState.tsx` | Delete the file; confirm `components/ui/index.ts` doesn't re-export it |
| M34 | **GigForm.tsx + FilterSheet.tsx still import `theme.colors.category[...]`** — dead access after switching to `category={cat.key}` Chip prop. Could be removed if no other code reads `theme.colors.category[...]` directly in those files | `apps/mobile/components/gig/GigForm.tsx`, `apps/mobile/components/ui/FilterSheet.tsx` | Verify and remove the unused references |

### Chat / messaging primitives (Stage 3 #23)

| # | Issue | File(s) | Why deferred / Fix |
|---|-------|---------|-------------------|
| M35 | **`ConversationItem` cannot render "You: " prefix** on outgoing-last-message previews — wireframe `messages.html .convo-preview .you` shows `You:` for previews where the last sender is the current user. `Conversation` schema (`packages/shared/src/types/chat.ts`) lacks `last_message_sender_id`, so we can't determine whether the current user authored the last message | `packages/shared/src/types/chat.ts`, `apps/mobile/components/chat/ConversationItem.tsx` | Add `last_message_sender_id: string \| null` to `Conversation`; populate from the `last message` join in `GET /v1/conversations`; in ConversationItem, prefix with `You: ` when `last_message_sender_id === currentUserId` |
| M36 | **`.tx`-styled previews not rendered** — wireframe shows transaction-event previews (e.g. "Escrow released · 0.80 SOL") in a distinct tone style. `Conversation.last_message` is plain text with no metadata to identify it as a system/tx event | `packages/shared/src/types/chat.ts`, `apps/mobile/components/chat/ConversationItem.tsx` | Add `last_message_kind: 'text' \| 'tx_event' \| 'system' \| null` to `Conversation`; render tx-event previews in mono with `--ok` tint |
| M37 | **Avatar tone tinting (`a-brand` / `a-accent` / `a-ok`) skipped** — wireframe `messages.html` colors avatars per thread for visual variety; `messages-chat.html` does not. Shipped the simpler/consistent version (plain `--inset`) — matches the chat-overview list. If visual variety is desired, derive tone deterministically from `other_user.id` hash | `apps/mobile/components/chat/ConversationItem.tsx` | Hash `other_user.id` → pick from `['default', 'brand', 'accent', 'ok']` and pass as `tone` prop on Avatar |
| M38 | **`MessageBubble` "me" bg deviates from wireframe `var(--brand)`** — wireframe uses solid brand fill (#2E5BD6 light / #5E87E8 dark) which read as harsh. Shipped softer brand-aligned blues: `#4365D2` light, `#3F5BA8` dark, plus a slightly lifted dark theirs (`#1B2231` instead of `--inset` raw `#131826`) | `apps/mobile/components/chat/MessageBubble.tsx` | Intentional UX deviation. Revisit with stakeholders if a brighter brand fill is desired; could expose as theme tokens (`chat.bubbleMe.bg.light`/`dark`) |
| M39 | **"Unread" / "Earlier" group headers** in chat-overview list not rendered — wireframe `messages-chat.html .conv group-header` divides the list. Screen-level grouping, not a primitive concern | `apps/mobile/app/(tabs)/messages.tsx` (Stage 4 #26) | Group conversations by `unread_count > 0` vs read at the screen level; render mono section labels between groups |
| M40 | **`MessageBubble` "Sending…" indicator** is an extrapolation — wireframe doesn't show this status. Kept as a small caption in `--ink-3` for functional feedback (user knows the message is in flight). Remove if wireframe-strict | `apps/mobile/components/chat/MessageBubble.tsx` | Remove if exact wireframe parity required |
| M41 | **`ChatContextDivider` sticky variant (`.ctx-sticky`)** not implemented — wireframe shows a 48h gig-reference card sticky at top of chat thread (square brand icon + eyebrow + title + chevron). Screen-level composition for Stage 4 #29 | `apps/mobile/app/chat/[userId].tsx` (Stage 4 #29) | Build inline at chat detail screen; not a generic primitive |

### Stage 3 #24 — feature components deferred to Stage 4 #29

| # | Issue | File(s) | Why deferred / Fix |
|---|-------|---------|-------------------|
| M42 | **`GigCTABar` not restyled to wireframe** — variants (worker accept / draft publish / accepted submit-proof / disputed) are role-specific and tied to gig-detail screen state. Wireframe spec: sticky 12/20/16 wrapper, 52h R16 brand primary + brand-tinted shadow, optional outline "Edit" + 52×52 inset overflow, `cta-sub` block (mono Escrow eyebrow + amount) for worker variant | `apps/mobile/components/gig/GigCTABar.tsx` | Restyle in lockstep with gig-detail screen translation |
| M43 | **`ExchangeCTABar` not restyled to wireframe** — same shape as GigCTABar but with success-tone variant ("Mark as paid") and dispute-state variant (outline "View dispute evidence", primary suppressed) | `apps/mobile/components/exchange/detail/ExchangeCTABar.tsx` | Restyle in lockstep with exchange-detail screen translation |
| ~~M44~~ | ~~`OfferSummaryCard` not restyled to wireframe~~ | ✅ Rewritten during Stage 4 #29 — border-only 18r card, status pill + pm chips row, mono `X SOL → Y CCY` rate row with arrow, sub line with rate-per-SOL + live-rate spread badge, divider + 2-col meta cells (Payment window / Tenda fee, switching to Escrow locked post-accept). Tenda fee computed live via `usePlatformConfigStore + computePlatformFee` |
| ~~M45~~ | ~~`PaymentWindowBanner` not restyled to wireframe~~ | ✅ Rewritten during Stage 4 #29 — 56h R14 horizontal row with leading 6px tone dot, body copy, trailing mono countdown. Warn-surface < 30 min, danger-surface on expiry, brand/info-surface otherwise. Buyer/seller copy variants. Countdown formatter switches between `mm:ss` and `Xh Ym` |
| M46 | **`TransactionMonitor` is a full-screen modal; wireframe wants a 56h inline banner** above the CTA bar — major API + UX change. Inline banner uses `--brand-surface` bg + spinner + body + mono signature. Modal with success/failure dialog is currently the only mode | `apps/mobile/components/feedback/TransactionMonitor.tsx` | Recommend: keep modal as terminal-failure fallback; add a new `TransactionMonitorBanner` inline-only component. Still open after Stage 4 #29 — gig-detail and exchange-detail both still mount the modal |

### Gradient rendering — RN native vs CSS wireframe

| # | Issue | File(s) | Why deferred / Fix |
|---|-------|---------|-------------------|
| M47 | **SVG radial gradients translate poorly from CSS** — wireframe uses CSS `radial-gradient(circle at 50% 15%, rgba(46,91,214,0.12), transparent 55%)` which renders as essentially clean off-white in browser preview (the `transparent N%` stop with `farthest-corner` default sizing produces a very subtle glow). My `react-native-svg` `<RadialGradient cx="50%" cy="15%" r="55%">` translation with `objectBoundingBox` units painted a far more saturated wash on real screens — visibly blue/brown enough that the user rejected it. Same alpha values, different sizing model. Auth funnel (welcome) backdrop ended up dropping the gradient entirely and shipping solid `surface.background` | `apps/mobile/components/auth/AuthBackdrop.tsx` (now deleted), `apps/mobile/app/(auth)/welcome.tsx` | Two viable paths: (a) compute gradient extent in pixels using window dimensions to mimic CSS `farthest-corner` sizing, with `gradientUnits="userSpaceOnUse"` and explicit fcx/fcy/r in pixels; (b) use a static decorative PNG asset of the gradient. Both involve effort that wasn't worth the visual gain for the auth backdrop. Re-evaluate if a screen genuinely benefits from the radial composition (e.g. wallet hero) |
| M48 | **No native linear-gradient in RN** — `expo-linear-gradient` is not installed. Wireframe uses `linear-gradient(145deg, var(--brand), #1E449F)` for the welcome logo square, `linear-gradient(145deg, accent → C9780C)` for avatar gradient fallbacks, brand-tinted hero card gradients on wallet, etc. Currently shipped: solid `brand.primary` (or `accent.primary`) fills as approximations. Avatar primitive `gradient='accent'\|'brand'` uses solid base colors instead of true 2-stop gradients | `apps/mobile/components/ui/Avatar.tsx`, `apps/mobile/app/(tabs)/wallet.tsx` (hero card pending), and any other gradient-fill spot | Install `expo-linear-gradient` (already an Expo SDK package), wrap with a small `<GradientFill>` helper that takes `from`/`to`/`angle`. Use throughout for true 2-stop gradients on logo squares, avatar fallbacks, wallet hero, etc. Cost: one new dep + ~30 LOC helper |
| M49 | **Wallet hero card not gradient-filled** — wireframe `wallet.html .hero` uses a stacked composition: `radial-gradient(circle at top right, rgba(46,91,214,0.10), transparent 60%)` over `linear-gradient(145deg, var(--brand-surface), var(--bg))` on a 148h R22 card with `--brand-border`. Currently shipped: solid `brand.primarySurface` over `--bg` background — flat brand wash with the brand-border outline. Visually close to the wireframe's intent but flat where the wireframe has gradient depth. Inherits the M47 (radial) + M48 (linear) constraints | `apps/mobile/app/(tabs)/wallet.tsx` | Solve M47 + M48 first (install `expo-linear-gradient` + add `<GradientFill>`/`<RadialGlow>` helpers with pixel-based sizing). Then layer them inside the hero card: linear bg + radial overlay top-right + brand-border ring |

### Wallet — error UX

| # | Issue | File(s) | Why deferred / Fix |
|---|-------|---------|-------------------|
| M50 | **Wallet balance silently shows `0.00 SOL` on RPC / API failure** — load() uses `Promise.allSettled` with per-promise `.catch()` that resolves to `setBalanceLamports(0)` / `setTransactions([])`. Solves the perpetual-skeleton bug but swallows the failure: a rate-limited or unreachable Solana RPC renders an authoritative-looking "0.00 SOL" with no signal that the balance call failed. Same for transactions ("No transactions yet"). User can't distinguish "actually empty" from "couldn't fetch" | `apps/mobile/app/(tabs)/wallet.tsx` | Track per-call error state (`balanceError`, `txError`); when set, render the hero amount as "—" plus a subtle warning chip ("Couldn't load — pull to retry") and the transactions list as an ErrorState with retry. Keep balance number visible only on actual success |

### Date / time formatting

| # | Issue | File(s) | Why deferred / Fix |
|---|-------|---------|-------------------|
| ~~M51~~ | ~~Day-grouping format inconsistency between wallet and chat~~ | ✅ Resolved during DRY refactor (D2/D10). `lib/date.ts` now exports `formatRelativeDay`, `formatRelativeShort`, `formatConvoTime`, `formatRelativeDayWithTime`, plus a generic `groupByDay` reducer. Wallet uses `groupByDay` + `formatRelativeDay`; chat uses `formatRelativeDay` via `ChatTimestampGroup`; `ConversationItem` uses `formatConvoTime`; `ReviewCard` uses `formatRelativeShort` |

## Open cross-stage questions (from STATUS.md)

| # | Question | Affected stage | Status |
|---|----------|----------------|--------|
| Q2 | **`SeekerWelcomeSheet` bullet copy is marked PROPOSE in wireframe** — confirm real copy from shipped component or approve wireframe copy | Stage 3 / 4 | open |
| Q3 | **Fee breakdown bps value (wireframe uses 1.25%)** — confirm real fee structure from `@tenda/shared` constants before rendering in create-gig fee calculator | Stage 4 #27 | open |

---

### Stage 4 #27–#31 — wireframe translation follow-ups

| # | Issue | File(s) | Why deferred / Fix |
|---|-------|---------|-------------------|
| M52 | **`gigs.submitted_at` column missing from schema** — `gigDeadlineMeta()` (lib/gig-display.ts) renders the "X to review" countdown for submitted gigs by using `gig.updated_at` as a proxy for the submission timestamp. Approximation works while the only post-accept actions that mutate the row are submission and proof additions, but is not deterministic — a poster's status update or any future column tweak could re-bump `updated_at` and shift the countdown | `packages/shared/src/db/schema.ts`, `apps/mobile/lib/gig-display.ts`, server submit route | Add `submitted_at: timestamp(...)` column to `gigs`; set it inside the submit transaction; switch the helper from `gig.updated_at` to `gig.submitted_at`. Requires migration |
| M53 | **`TransactionMonitor` still mounts as full-screen modal across detail screens** — gig-detail and exchange-detail both render up to three `<TransactionMonitor>` components (setup-tx, accept-tx, main-tx); each opens a blocking modal on confirmation. Wireframe spec (M46) wants a 56h inline brand-surface banner above the CTA bar that auto-dismisses on confirmation, with the modal kept only for terminal failure | `apps/mobile/components/feedback/TransactionMonitor.tsx`, `apps/mobile/app/gig/[id]/index.tsx`, `apps/mobile/app/exchange/[id]/index.tsx` | Add a `<TransactionMonitorBanner>` inline-only component that subscribes to the same signature/onConfirmed/onFailed API but renders inline; keep the modal as terminal-failure fallback. Migrate detail screens to use the banner |
| M54 | **`FloatingChromeButton` uses solid translucent inset instead of BlurView** — `components/ui/FloatingChromeButton.tsx` ships a `surface.card` fill with a hairline border because `expo-blur` is not installed (M1). Wireframe spec calls for `backdrop-filter: blur(20px)` over scroll content. Visually fine on solid backgrounds but loses the wireframe's glassy treatment when content scrolls beneath the chrome | `apps/mobile/components/ui/FloatingChromeButton.tsx` | Solve M1 first (install `expo-blur`). Then wrap the button bg in a `<BlurView intensity={20} tint="default">`. Comment in the file already notes this |
| M55 | **`AccountCard` icon classification is regex-heuristic** — exchange-detail's inline `AccountCard` picks the icon (Building2 vs Smartphone) by matching the method name against a regex (`/opay|palmpay|kuda|airtel|mtn|m-?pesa|wallet|momo/`). Reasonable for current Nigerian/Kenyan/Ghanaian methods but can misclassify regional or future methods (e.g. EU SEPA, US ACH, USDC wallet types) | `apps/mobile/app/exchange/[id]/index.tsx` | Move the classification to `@tenda/shared` constants — derive `kind: 'bank' \| 'mobile_money' \| 'crypto' \| ...` from method metadata at the type level. Wire backend `user_exchange_accounts.method` to a typed enum |
| M56 | **`Lint`: 19 `react/no-unescaped-entities` errors** — pre-existing JSX literal apostrophes/quotes across `connect-wallet`, `support/glossary`, `support/index`, `support/wallet`, `create-gig`, `settings`, `+not-found`, `chat/[userId]`, `exchange/[id]/index`, `gig/[id]/edit`, `gig/[id]/index`, `MessageBubble`, `ExchangeOfferForm`, `OfferSummaryCard`, `useExchangeActions`, `SearchSheet`, `SeekerWelcomeSheet`. Mostly contractions like `we're` / `don't` / `"X"` in copy. Cosmetic — does not affect runtime | (multiple, see lint output) | Bulk autofix: replace `'` with `’` typographic apostrophe (preferred) or `&apos;` HTML entity in JSX text. Re-run `pnpm lint` and confirm zero errors |
| M57 | **`Lint`: ~12 `import/no-duplicates` warnings** for `@tenda/shared` — files importing both a value (`computePlatformFee`, `LAMPORTS_PER_SOL`) and a type (`type ExchangeOffer`) from `@tenda/shared` get flagged because each `import` statement is a separate line. Affects `PaymentInput`, `Classic/PriceLeading/Rich`, `GigForm`, `ExchangeOfferForm`, `OfferSummaryCard`, `useExchangeActions`, `gig/[id]/edit`, plus a few others | various | Merge each file's two imports into one (`import { computePlatformFee, LAMPORTS_PER_SOL, type ExchangeOffer } from '@tenda/shared'`). Mechanical fix |
| M58 | **`Lint`: ~18 array-type warnings** (`Array<T>` → `T[]`) — pre-existing style preference in `GigActionSheets`, `GigForm`, `DurationPicker`, `Badge`, `connect-wallet`. Cosmetic | various | Run `pnpm lint --fix`; the rule is auto-fixable |
| M59 | **`Lint`: 5 `react-hooks/exhaustive-deps` warnings** — `ServerStatus`, `Spinner`, `Drawer`, `Toast`. Pre-existing intentional deps: `Drawer` lifts shared values into closures that don't need to re-trigger; `Toast` uses a mutable ref by design | various | Audit each call site individually; either add the dep with proper memoisation or `// eslint-disable-next-line` with a reason. Three of these are pre-existing and intentional |
| M60 | **PaymentMethods description visibility tradeoff documented** — `AccountCard` was deliberately expanded beyond wireframe spec (single 72h row with masked detail) to a multi-section card showing the full account number in mono 18/700, holder name as a separate copy chip, and brand-filled prominent Copy CTA. Reason: production buyers need the full account number visible without unmasking, and need to verify the holder name independently. Documented as accepted deviation | `apps/mobile/app/exchange/[id]/index.tsx` | None (intentional). Revisit if design feedback wants the compact masked variant |
| M61 | **`+not-found.tsx` does not match a specific wireframe** — no `not-found.html` in the wireframe set. Shipped a brand-aligned composition (72×72 brand-tinted Compass icon, mono `404` eyebrow, large title, descriptive caption, Back-to-home primary CTA). Aligns with the wireframe's empty-state pattern (§3.11) | `apps/mobile/app/+not-found.tsx` | None (extrapolation). Confirm with design or accept |
| M62 | **`SectionLabel` was retained as a separate component despite Eyebrow extraction (D7)** — `Eyebrow` is the typography primitive (just text styles), `SectionLabel` wraps it with the wireframe form-section padding (18/8/20). Two-component setup means callers need to choose. Mostly clear, but easy to grab the wrong one | `apps/mobile/components/ui/Eyebrow.tsx`, `apps/mobile/components/ui/SectionLabel.tsx` | Document the split in a one-line JSDoc on each: "for in-card eyebrows, use Eyebrow with custom margin; for between-section eyebrows, use SectionLabel" |
| M63 | **Server `PATCH /v1/gigs/:id` rejects orphan country/city pair instead of cascading** — unlike `PATCH /v1/users/:id` (which auto-nulls the orphan city), the gigs PATCH route in `routes/v1/gigs/_id/index.ts` rejects with 400 when country/city are inconsistent. Inconsistent behaviour for what is essentially the same data-shape problem | `apps/server/src/routes/v1/gigs/_id/index.ts` | Either: (a) cascade on PATCH gigs to match users (soft auto-null), keeping POST gigs strict; or (b) make users PATCH equally strict (reject 400). Pick one model and apply consistently |
| M64 | **No automatic country derivation from city in `GigForm`** — `update-profile.tsx` already does `findCountryForCity(user.city)` when initializing state. `GigForm` doesn't — if a gig is created from a city dropdown only, country is null until the user opens the country picker | `apps/mobile/components/gig/GigForm.tsx` | When `setSelectedCity` fires without a known country, call `findCountryForCity` to auto-fill country. Defensive — currently the picker requires both, so likely a no-op in practice |
| M65 | **Exchange-detail `AccountCard` doesn't fall back when `payment_accounts` is empty for a draft offer** — pre-publish, the seller has no accounts yet (or has draft accounts not yet attached). Currently the section just doesn't render. Wireframe doesn't address this state explicitly | `apps/mobile/app/exchange/[id]/index.tsx` | Render an empty-state row prompting the seller to add a payment account before publishing. Or block publish until ≥ 1 account is attached at the form level |
| M66 | **Exchange offer `accept_deadline` not surfaced in OfferSummaryCard** — old card surfaced it as a third meta cell. New V2 card uses 2-col meta (Payment window / Tenda fee or Escrow locked / Tenda fee). The deadline now only appears as a status implicitly | `apps/mobile/components/exchange/detail/OfferSummaryCard.tsx` | Decide whether to surface accept-deadline as a row inside the card, a chip in the top row, or a banner below. Currently relying on the `expired` status to communicate the outcome — acceptable but loses the upfront countdown |
| M67 | **`useGracePeriodSeconds()` triggers a fetch on every list render** — every `GigCardCompact` instance calls `useGracePeriodSeconds()` and inside the hook `useEffect` checks `grace == null` before fetching. The fetch is de-duped via the inflight Promise in the store, so only one network call fires, but each card still mounts an effect. Negligible but suboptimal for large feeds | `apps/mobile/stores/platform-config.store.ts`, all `GigCardCompact` variants | Hoist the fetch one level up — call once at the FlatList parent (home/my-gigs/exchange) and let cards read the cached value via `usePlatformConfigStore((s) => s.config?.grace_period_seconds)` directly without their own effect |
| M68 | **No `useGigDeadline()` hook with auto-tick** — `gigDeadlineMeta()` is a pure function that computes a snapshot; the cards don't re-render when "45m left" should become "44m left". For low-traffic lists this is fine (re-render on focus refreshes the labels), but for a urgent-deadline gig the time can drift | `apps/mobile/lib/gig-display.ts`, all `GigCardCompact` variants | Add a `useGigDeadline(gig, gracePeriodSeconds)` hook that uses `setInterval(60_000)` to re-render the chip; only schedule the interval when the gig is in a counting status (`open` / `accepted` / `submitted`); throttle the interval up to 1s for urgent (<5 min) and down to 60s otherwise |
| M69 | **`exchange-detail` review handle is derived as `@${seller.first_name?.toLowerCase()}`** — same shortcut as gig-detail. Matches the wireframe's narrative ("Reviews for @rashim") but is fake — we don't store usernames, only first/last name. Will produce duplicate handles for users sharing first names | `apps/mobile/app/exchange/[id]/index.tsx`, `apps/mobile/app/gig/[id]/index.tsx` | Add a real `username` / `handle` column to users (or derive a deterministic short handle from `wallet_address`). Then thread it through to the review section title |
| M70 | **No `submitted_at`-equivalent for exchange offers** — `exchange_offers` has `accepted_at` but no timestamp for the buyer's mark-paid action. PaymentWindowBanner counts down from `accepted_at + payment_window_seconds`, but post-mark-paid there's no equivalent timer for "seller release window". Currently the seller side just sees "Awaiting buyer payment" without a countdown until the deadline passes | `packages/shared/src/db/schema.ts` (exchange_offers), `PaymentWindowBanner` | Add `paid_at: timestamp(...)` column on `exchange_offers`; add a `release_window_seconds` config; show a release countdown for the seller after buyer marks paid. Requires migration |
| M71 | **Profile reputation conversion divides by 20 client-side** — `app/(tabs)/profile.tsx` displays reputation as `(score / 20).toFixed(1)` because backend stores 0–100 but UI shows 0–5 stars. The conversion factor is duplicated wherever stars are rendered (currently just profile, but reviews and PersonCard could need it too) | `apps/mobile/app/(tabs)/profile.tsx`, `apps/mobile/lib/...` | Add a `formatReputation(score)` helper to `lib/gig-display.ts` (or a new `lib/user.ts`) returning `{ stars: number, display: string }` so the conversion lives in one place |
| M72 | **`profile.tsx` `My gigs` count shows `2 active`** — `activePosterGigs` filters posted gigs by `open / accepted / submitted`. Wireframe shows `2 active` for poster, `5 active` for seeker — implies the seeker count includes worker gigs they're working on. Currently only counts posted-as-poster | `apps/mobile/app/(tabs)/profile.tsx` | Decide whether the count means "gigs I posted that are still open" (current) or "gigs I'm involved in that are still active" (poster + worker side); update if the seeker meaning was intended |
| M73 | **Open `(support)/escrow.tsx` fee calculator only formats fiat** — input box accepts a fiat amount (NGN/USD/whatever) but the platform fee on-chain is computed in lamports against SOL. The breakdown shows "Tenda fee (2%)" and "Worker receives" both as fiat amounts, which is technically correct only if the SOL/fiat rate is exactly the entered amount — fine for explanation, but doesn't reflect actual on-chain rounding | `apps/mobile/app/(support)/escrow.tsx` | Either: (a) note "approximate" alongside the breakdown; (b) require a SOL input and convert via the live exchange rate. Current copy already says "rate subject to change" — acceptable |
| M74 | **Verification pass blockers (#31): only mobile + server + shared type-check + mobile lint were run. No e2e / dev-client QA done in this pass** — type-check is clean across all three workspaces. Lint reports 19 errors + 35 warnings (all in the M56–M59 buckets). Dev-client smoke pass deferred — needs a person on a device | Stage 4 #31 | Run `pnpm dev-client` on Android, walk every screen translated in stages 4 #25–#30 (auth funnel, all tabs, hidden tab routes, detail modals, edit screens, chat, support group, +not-found). Document any device-only regressions here |

### Landing v2 — backend dependencies (referenced from `apps/tendahq/LANDING_TODO.md`)

| # | Issue | File(s) | Why deferred / Fix |
|---|-------|---------|-------------------|
| M75 | **Public 24h stats endpoint missing** — landing §02 trust strip and §11 footer assume `GET /v1/public/stats/24h` returning `{ volume_usd, volume_delta_pct, settlements }`. Currently flagged via `<Placeholder issue="M75">` everywhere in the landing | server `routes/v1/public/*` (new) | Add the route. Cache 5 min server-side (mirror `getExchangeRates` pattern). Wire the frontend hook + drop the `Placeholder` wrapper |
| M76 | **Public rolling stats endpoint missing** — landing §01 hero eyebrow + §06 pillars + §10 receipts assume `GET /v1/public/stats/rolling` returning `{ avg_settlement_seconds, dispute_rate_bps, gigs_this_week, countries_live }`. Flagged via `<Placeholder issue="M76">` | server (new) | Add the route. Stat queries should be aggregated, not per-row. Same caching pattern as M75 |
| M77 | **Public live-feed endpoint missing** — landing §02 proof band + §05 ticker + §07 coverage map all use `MOCK_PROOF_FEED` / `MOCK_TICKER_ROWS` arrays. Need `GET /v1/public/feed/live` (or SSE) returning recent settlements `[{ id, verb, amount_lamports, fiat?, sig, ts }]` | server (new) | Tail the recent settlements; redact PII; cap at last 50. Frontend ticker subscribes via fetch+poll (no SSE on a static SPA) |
| M78 | **Public coverage endpoint missing** — landing §07 coverage map uses placeholder ping coordinates. Need `GET /v1/public/coverage` returning per-country `{ code, last_settlement_ts, count_24h }` | server (new) | Aggregate per country from settlements. Used to drive the §07 world-map pings |
| M79 | **iOS waitlist email capture has no backend** — landing §10 final CTA captures email; v1 falls back to `mailto:` | server `routes/v1/public/waitlist` (new) | Either `POST /v1/public/waitlist/ios { email }` to a list, or wire to a hosted form provider (Buttondown, Formspree) |
| ~~M80~~ | ~~Fee model conflict between landing copy and live `/v1/platform/config`~~ | landing-wide | ✅ Resolved during landing build — fee values pulled live from `useFeePercents()` hook. Marketing copy never hardcodes fee numbers |
| M81 | **Public-stats endpoints need rate-limit + 5-min server cache** — once M75/M76/M77 land, the marketing site shouldn't hammer prod DB. Pattern matches `getExchangeRates` | server lib (new) | Wrap stats endpoints in a memoized loader with 5-min TTL + per-IP rate-limit. Document on each route |
| M82 | **Placeholder numerics in `apps/tendahq/src/data/stats.ts`** — every wireframe number that doesn't come from a live endpoint is wrapped in `<Placeholder issue="M75/M76">`. Audit before public launch: `grep -rn 'data-placeholder="true"' apps/tendahq/dist` should return zero hits | `apps/tendahq/src/data/stats.ts`, `<Placeholder>` consumers | Wire each placeholder to its real endpoint as M75/M76/M77 land. Mark resolved in this issue once `dist` is clean |
| M83 | **`/v1/platform/exchange-rates` payload missing GHS** — currency strip falls back to "—/SOL" gracefully but loses the GHS rate visually | server `lib/exchange-rates.ts` | Confirm CoinGecko key for GHS or add a fallback source (CFA-pegged, manual rate). Currently silently dropped from response |
| M84 | **`VITE_API_BASE_URL` not set in production deploy** — landing site `apps/tendahq` will fail to boot if env var is missing (`src/env.ts` throws). Tracked in `.env.example`; per-environment override in `.env.development.local` | `apps/tendahq/.env.example`, infra | Set in Vercel project env (or hosting equivalent) before promotion to prod. Confirm `https://api.tenda.so` is the canonical prod URL |

### Escrow contract — fallback / state-machine confirmations (driven by landing §04 fact-check)

The user reviewed the §04 "fallback" copy on the landing and flagged that the wireframe was **guessing at state-machine behaviour**. These items are product confirmations for the escrow contract — every claim made in §04's Fallback panel needs to map to a real instruction in the on-chain program before public launch.

| # | Issue | File(s) | Why / Fix |
|---|-------|---------|-----------|
| M85 | **Audit row in §04 is honest now** — landing says "Pre-mainnet · audit pending". Before public mainnet launch, either ship a real audit + update the row to reflect the firm/date, or remove the audit row entirely | `apps/tendahq/src/components/sections/how-escrow-works/content.ts`, contract repo | Engage audit firm pre-mainnet; once final report is published, replace the placeholder string with the real firm + date |
| M86 | **Auto-approve after review window must be implemented** — landing §04 Route B claims "If the poster (or seller) doesn't approve or dispute within the review window, the program auto-releases SOL to the worker (or buyer)". Window is currently described as 48h. Without this protection a non-responsive poster can strand a worker's payout indefinitely | escrow program, server, mobile | Add a `auto_approve` instruction the worker (or anyone, permissionless) can call once `now > submitted_at + auto_approve_window`. The window should be a `platform_config` field (`auto_approve_window_seconds`, default 172800 = 48h). Server exposes it via `/v1/platform/config`; mobile shows a countdown post-submit |
| M87 | **Cancel pre-accept must be poster/seller-initiated** — landing §04 Route A says only the locker can cancel before a worker (or buyer) accepts. Confirm the contract gates `cancel` on `state === draft || open`, and rejects from `accepted` onwards. The mobile app already exposes a `Cancel Offer` / `Cancel Gig` button in the open state — verify the program actually returns SOL atomically and not via a separate refund step | escrow program, `useExchangeActions`, gig actions | Confirm contract path; if missing, add a `cancel_pre_accept` instruction. Audit mobile UI gate so the button only appears in legal states |
| M88 | **Reclaim after proof-deadline must be poster/seller-claimable** — landing §04 Route C says "Worker (or buyer) misses the proof-submission deadline → poster (or seller) can claim the refund". The mobile app currently has `Claim Refund` for expired exchange offers (verified in `ExchangeCTABar.tsx:169` — `actions.refundExpired`). Confirm gigs have the equivalent on the contract side and surface it in the gig-detail CTA | escrow program, gig CTA bar, server | Confirm contract has a `reclaim_expired` instruction gated on `now > deadline`. Add the equivalent CTA on gig-detail when the gig has expired without proof |
| M89 | **Dispute precondition — post-proof only** — landing §04 Route D states disputes can only be opened **after proof is submitted**. The current mobile UI exposes `Dispute` from both `accepted` and `paid` states (per `ExchangeCTABar.tsx:148,155`). Two interpretations: (a) the wireframe is the canonical product spec and disputes shouldn't open from `accepted`; (b) the mobile UI is correct and the wireframe needs to relax the wording. Pick one and align UI + contract + landing copy | escrow program, mobile CTA bars, landing §04 | Decide policy. If post-proof-only: gate the dispute action and update mobile UI. If pre-proof allowed: update landing copy to match |
| M90 | **Dispute resolution mechanic — split vs binary award** — landing copy says "Tenda reviewers split the locked amount based on evidence". Confirm this is what the program actually supports: does the dispute resolution path allow proportional splits, or only binary `release_to_worker | refund_to_poster`? | escrow program, dispute mediation lib | Confirm spec. If binary-only, soften landing copy to match. If split is supported, confirm the resolver UI in admin can specify an exact split percentage |
| M91 | **Dispute SLA — `≤ 24h` claim needs an enforcement plan** — landing §04 Route D promises ≤ 24h dispute resolution. Currently dispute mediation is human-driven (Phase 3 admin lib). No timer is published or enforced | server, admin UI | Define the SLA. Either: (a) commit to the 24h SLA and add ops alerts when a dispute thread is approaching the limit; (b) soften the landing copy to "human-reviewed, typically <72h" |
| M92 | **Settlement-time claim — "under 2 seconds"** — §04 Stage 04 body states "Settlement under 2 seconds". This depends on Solana block-finality + program execution. Should match what we'd surface in a public stats dashboard (M76's `avg_settlement_seconds`). Currently inconsistent: landing also has `<2s` in the hero stat-row | landing-wide | Once M76 lands, derive the claim from the rolling endpoint. Until then, document `<2s` as a typical-finality claim, not a guaranteed SLA |
| M93 | **Source repo link — `tenda-escrow` is referenced but not public** — §04 Fallback panel says "Simplified. Full source in the `tenda-escrow` repo." `APP_INFO.githubUrl` is currently `'#'` | `apps/tendahq/src/app-info.ts`, repo visibility | Either: (a) push the program crate to a public repo and wire the URL, or (b) replace the footnote with "audit-time release planned" until the repo is public |
